# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

**ztlctl** (Zettelkasten Control) is a CLI utility for managing a Zettelkasten note-taking system. The project is in early development.

- **Repository**: github.com/ThatDevStudio/ztlctl
- **Main branch**: `develop`

## Git Workflow

- The default branch is `develop`, not `main`
- Target PRs against `develop`
