---
phase: 25-new-v3-0-feature-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/contradiction-detection.md
  - docs/media-ingestion.md
autonomous: true
requirements:
  - NDOC-03
  - NDOC-04

must_haves:
  truths:
    - "User can read a contradiction-detection.md page covering heuristic scoring, CAT_SEMANTIC check, confirm_contradiction, graph edges, and MCP review resource"
    - "User can read a media-ingestion.md page with prominent optional-dependency callout, format coverage, ingest_media CLI/MCP usage, and captured-to-annotated workflow"
  artifacts:
    - path: "docs/contradiction-detection.md"
      provides: "Contradiction detection feature documentation"
      contains: "check_contradictions"
    - path: "docs/media-ingestion.md"
      provides: "Media ingestion feature documentation"
      contains: "ingest_media"
  key_links:
    - from: "docs/contradiction-detection.md"
      to: "src/ztlctl/services/contradiction.py"
      via: "CLI flags and scoring details verified against source"
      pattern: "check_contradictions|confirm_contradiction|CAT_SEMANTIC"
    - from: "docs/media-ingestion.md"
      to: "src/ztlctl/services/ingest.py"
      via: "CLI flags and config verified against source"
      pattern: "ingest_media|faster-whisper|ingest\\.media"
---

<objective>
Write two standalone v3.0 feature documentation pages: contradiction-detection.md and media-ingestion.md.

Purpose: Users and agents need discoverable documentation for contradiction detection (semantic integrity) and media ingestion (transcription pipeline). Media ingestion has an optional dependency (faster-whisper) that needs a prominent callout.
Output: Two complete docs pages in docs/ following Documentation Conventions from CLAUDE.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-new-v3-0-feature-pages/25-CONTEXT.md
@CLAUDE.md (Documentation Rules and Documentation Conventions sections)
@docs/plugins.md (established page pattern)
@mkdocs.yml (nav structure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write contradiction-detection.md (NDOC-03)</name>
  <files>docs/contradiction-detection.md</files>
  <read_first>
    - src/ztlctl/services/contradiction.py (ContradictionService — scoring, candidate discovery)
    - src/ztlctl/controllers/contradiction.py (ContradictionController — CLI wiring)
    - src/ztlctl/actions/_check.py (check_contradictions, confirm_contradiction ActionDefinitions)
    - src/ztlctl/mcp/resources.py (ztlctl://review/contradictions resource definition)
    - src/ztlctl/services/check.py (CAT_SEMANTIC integration)
    - docs/plugins.md (established page structure pattern)
    - CLAUDE.md (Documentation Conventions section)
  </read_first>
  <action>
Create docs/contradiction-detection.md as a How-to guide (per Diataxis).

Run `uv run ztlctl check --help` to find contradiction-related flags. Check if there is a dedicated `ztlctl contradiction` or similar subcommand. Verify all parameter names against source.

Page structure:

1. **Frontmatter**: `title: Contradiction detection`
2. **H1: Contradiction detection** — 2-3 sentence overview: surface notes that may contradict each other using heuristic scoring, confirm contradictions as graph edges
3. **H2: How contradiction detection works** — candidate pair discovery: shared topic, high vector similarity, decision note type. Heuristic scoring: negation patterns, key_points comparison. How candidates are ranked.
4. **H2: Running contradiction checks** — CLI usage with exact flags from --help. Show example invocation and output. Explain CAT_SEMANTIC category in `ztlctl check`.
5. **H2: Confirming contradictions** — confirm_contradiction action: what it does (records `contradicts` edge in graph), CLI usage, example
6. **H2: Graph edges** — how confirmed contradictions appear in the graph, querying them
7. **H2: MCP tools and resources** — table of check_contradictions and confirm_contradiction tools with parameters, plus ztlctl://review/contradictions resource (what it returns, how agents consume it)
8. **H2: Agent review workflow** — concrete example: agent reads review/contradictions resource, evaluates candidates, confirms or dismisses
9. **H2: What's next** — links to concepts.md, agentic-workflows.md, commands.md

Admonitions: use only warning/note/tip. Include a note about vector index requirement for scoring.
  </action>
  <verify>
    <automated>test -f docs/contradiction-detection.md && grep -q "check_contradictions" docs/contradiction-detection.md && grep -q "confirm_contradiction" docs/contradiction-detection.md && grep -q "ztlctl://review/contradictions" docs/contradiction-detection.md && grep -q "CAT_SEMANTIC\|semantic" docs/contradiction-detection.md && grep -q "What's next" docs/contradiction-detection.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at docs/contradiction-detection.md
    - Contains frontmatter with `title: Contradiction detection`
    - Documents heuristic scoring approach (negation patterns, key_points)
    - Documents CAT_SEMANTIC check category
    - Documents confirm_contradiction action with graph edge recording
    - CLI flag names match source output exactly
    - Documents ztlctl://review/contradictions MCP resource
    - Has agent review workflow example
    - Ends with "What's next" section with 2-3 relative links
    - Uses only warning/note/tip admonitions
    - Headings are sentence case
  </acceptance_criteria>
  <done>contradiction-detection.md is a complete How-to page covering semantic integrity with verified CLI flags, MCP tools/resource, agent workflow, and cross-references</done>
</task>

<task type="auto">
  <name>Task 2: Write media-ingestion.md (NDOC-04)</name>
  <files>docs/media-ingestion.md</files>
  <read_first>
    - src/ztlctl/services/ingest.py (IngestService — ingest_media method, supported formats)
    - src/ztlctl/controllers/ingest.py (IngestController — CLI wiring)
    - src/ztlctl/actions/_ingest.py (ingest_media ActionDefinition)
    - src/ztlctl/config/settings.py (MediaIngestConfig or [ingest.media] config section)
    - src/ztlctl/services/transcription.py (TranscriptionService if it exists — whisper integration)
    - CLAUDE.md (Documentation Conventions)
  </read_first>
  <action>
Create docs/media-ingestion.md as a How-to guide (per Diataxis).

Run `uv run ztlctl ingest --help` (and subcommands) to capture exact CLI interface. Verify all parameter names against source.

Page structure:

1. **Frontmatter**: `title: Media ingestion`
2. **H1: Media ingestion** — 2-3 sentence overview: ingest media files and transcripts into the vault as structured captured references
3. **H2: Prerequisites** — `!!! warning` callout: faster-whisper is an optional dependency, must be installed separately for audio/video transcription. Show install command. Note that transcript files (txt, vtt, srt) do NOT require faster-whisper.
4. **H2: Supported formats** — table of supported media formats (mp4, mp3, m4a, wav) and transcript formats (txt, vtt, srt) with notes on each
5. **H2: Ingesting media files** — CLI usage for audio/video with exact flags, example invocation, what happens (local transcription via whisper, no data leaves machine)
6. **H2: Ingesting transcripts** — CLI usage for pre-existing transcript files, example
7. **H2: The two-phase workflow** — captured reference → annotated: explain that ingest creates a `captured` reference with source bundle (normalized_text, capture_agent, modalities), then an agent or user annotates it to `annotated` status
8. **H2: Configuration** — `[ingest.media]` TOML section: whisper model selection, language hints, output preferences. Show example config with exact field names from MediaIngestConfig.
9. **H2: MCP tool** — ingest_media tool: parameters, return format, how agents use it
10. **H2: What's next** — links to concepts.md, configuration.md, agentic-workflows.md

The `!!! warning` for faster-whisper MUST be prominent (near the top of the page, not buried).
  </action>
  <verify>
    <automated>test -f docs/media-ingestion.md && grep -q "faster-whisper" docs/media-ingestion.md && grep -q "ingest_media" docs/media-ingestion.md && grep -q "captured" docs/media-ingestion.md && grep -q "annotated" docs/media-ingestion.md && grep -q '!!! warning' docs/media-ingestion.md && grep -q "What's next" docs/media-ingestion.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at docs/media-ingestion.md
    - Contains frontmatter with `title: Media ingestion`
    - Has prominent `!!! warning` callout for faster-whisper optional dependency near top
    - Documents all supported formats (mp4, mp3, m4a, wav, txt, vtt, srt)
    - CLI flag names match `uv run ztlctl ingest --help` output exactly
    - Documents two-phase captured-to-annotated workflow
    - Documents [ingest.media] config section with field names from source
    - Documents ingest_media MCP tool
    - Ends with "What's next" section with 2-3 relative links
    - Uses only warning/note/tip admonitions
    - Headings are sentence case
  </acceptance_criteria>
  <done>media-ingestion.md is a complete How-to page with prominent optional-dep warning, format coverage, verified CLI flags, two-phase workflow, config reference, MCP tool, and cross-references</done>
</task>

</tasks>

<verification>
Both pages exist in docs/ with correct frontmatter, verified CLI flags, MCP tool/resource references, agent workflow examples, and "What's next" cross-references. Media ingestion has prominent faster-whisper warning. All headings are sentence case. Only warning/note/tip admonitions used.
</verification>

<success_criteria>
- docs/contradiction-detection.md covers NDOC-03 requirements completely
- docs/media-ingestion.md covers NDOC-04 requirements completely
- Every CLI flag name verified against `uv run ztlctl <command> --help`
- Every MCP tool/resource name verified against ActionDefinitions and resources.py
- Pages follow Documentation Conventions from CLAUDE.md
- Media ingestion page has prominent faster-whisper optional dependency callout
</success_criteria>

<output>
After completion, create `.planning/phases/25-new-v3-0-feature-pages/25-02-SUMMARY.md`
</output>
