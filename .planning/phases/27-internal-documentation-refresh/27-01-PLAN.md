---
phase: 27-internal-documentation-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - README.md
autonomous: true
requirements:
  - IDOC-01
  - IDOC-03

must_haves:
  truths:
    - "CLAUDE.md architecture section lists all current services, controllers, and action counts"
    - "CLAUDE.md describes feature-local action registration pattern and centralized PluginManager"
    - "README.md features list includes session recall, polaris, contradiction detection, and media ingestion"
    - "README.md command table is complete and accurate for v3.0"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated architecture section with v3.0 service/controller/action inventory"
      contains: "feature-local action registration"
    - path: "README.md"
      provides: "Updated features list and command examples for v3.0"
      contains: "session recall"
  key_links:
    - from: "CLAUDE.md"
      to: "src/ztlctl/services/"
      via: "service class inventory"
      pattern: "services/"
    - from: "README.md"
      to: "docs/"
      via: "documentation links"
      pattern: "docs/"
---

<objective>
Update CLAUDE.md architecture section and README.md to accurately describe the v3.0 system.

Purpose: Developers and contributors reading CLAUDE.md get an accurate architecture overview with correct service/controller/action counts and v3.0 patterns. New contributors reading README.md see all current features including session recall, polaris, contradiction detection, and media ingestion.

Output: Updated CLAUDE.md and README.md files reflecting v3.0 reality.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md architecture section with v3.0 inventory (IDOC-01)</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md (full file — the architecture section starts at line 203),
    src/ztlctl/services/ (list files to count services),
    src/ztlctl/controllers/ (list files to count controllers),
    src/ztlctl/actions/ (list files and count ActionDefinition occurrences),
    src/ztlctl/plugins/manager.py (centralized PM factory pattern),
    src/ztlctl/commands/generator.py (CLI generation from ActionRegistry)
  </read_first>
  <action>
    Update the ## Architecture section of CLAUDE.md (starting at line 203) to reflect v3.0 reality. Per IDOC-01:

    1. Replace the current bullet-list architecture overview with a comprehensive section that includes:

    2. **Service inventory**: List all service classes by counting `class *Service` in `src/ztlctl/services/`. The requirement says "15 services" — verify by counting (exclude BaseService and any docstring examples). List them: CreateService, QueryService, GraphService, SessionService, ReweaveService, CheckService, UpdateService, ExportService, InitService, VectorService, WorkflowService, ContradictionService, RecallService, IngestService, TranscriptionService (plus ContextService, DocsService, UpgradeService, SourceBundleService if present — count actual classes).

    3. **Controller inventory**: List all controller classes by counting `class *Controller` in `src/ztlctl/controllers/`. The requirement says "17 controllers" — verify by counting (exclude BaseController). List the concrete controllers.

    4. **Action count**: Count ActionDefinition instances across `src/ztlctl/actions/_*.py` files. The requirement says "73+ actions" — verify by summing counts per file.

    5. **Feature-local action registration**: Describe the pattern where ActionDefinitions live in `src/ztlctl/actions/` modules colocated by feature area (_creation.py, _query.py, _graph.py, _session.py, _check.py, _export.py, _ingest.py, _lifecycle.py, _admin.py), not in a single monolithic registry file.

    6. **Centralized PluginManager**: Describe the PM factory in `src/ztlctl/plugins/manager.py` — single construction point, config injection, entry-point discovery.

    7. **Package structure**: Update the existing package listing to include `controllers/` and `actions/` directories that were added in v3.0.

    Keep the existing content above and below the Architecture section unchanged. The section should be concise (not a wall of text) — a developer should be able to scan it in 30 seconds.

    Do NOT change any other section of CLAUDE.md.
  </action>
  <verify>
    <automated>grep -c "feature-local" CLAUDE.md && grep -c "PluginManager" CLAUDE.md && grep -c "controllers/" CLAUDE.md && grep -c "actions/" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "feature-local" CLAUDE.md returns at least 1 match
    - grep "PluginManager" CLAUDE.md returns at least 1 match
    - grep "controllers/" CLAUDE.md returns at least 1 match
    - grep "actions/" CLAUDE.md returns at least 1 match
    - grep "ActionDefinition" CLAUDE.md returns at least 1 match
    - grep "services/" CLAUDE.md still present (not removed)
    - The service count, controller count, and action count in CLAUDE.md match actual source file counts
  </acceptance_criteria>
  <done>CLAUDE.md architecture section lists all services, controllers, and actions with accurate counts; describes feature-local registration and centralized PluginManager factory</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md features and commands for v3.0 (IDOC-03)</name>
  <files>README.md</files>
  <read_first>
    README.md (full file),
    src/ztlctl/actions/_session.py (session recall action names),
    src/ztlctl/actions/_check.py (contradiction/polaris action names),
    src/ztlctl/actions/_ingest.py (ingestion action names),
    docs/session-recall.md (for accurate feature description),
    docs/polaris.md (for accurate feature description),
    docs/contradiction-detection.md (for accurate feature description),
    docs/media-ingestion.md (for accurate feature description)
  </read_first>
  <action>
    Update README.md to reflect v3.0 features. Per IDOC-03:

    1. **Features list** (line ~73): Add bullet points for v3.0 features that are currently missing:
       - Session recall (temporal, topic, topology queries across session history)
       - Polaris priorities (persistent strategic alignment layer, MCP resource, check_alignment)
       - Contradiction detection (semantic integrity checking, heuristic scoring, graph edges)
       - Media ingestion (faster-whisper transcription, 11 format support, captured-to-annotated workflow)
       - Methodology guidance (prose-as-title convention, title quality checks)
       - Event model (WAL-backed reliable dispatch, service-only post_action)
       - Action registry (feature-local definitions, auto-generated MCP + CLI surfaces)

    2. **Features list**: Update existing bullets that reference outdated descriptions. The "Text-first ingestion" bullet should be updated to include media ingestion. The "MCP server" bullet should reference accurate tool counts (73+ tools).

    3. **Quick Start** (line ~37): Add 1-2 lines showing v3.0 commands after the existing example:
       - `ztlctl agent session recall-topic "async patterns"` (recall)
       - `ztlctl check contradictions` (contradiction detection)

    4. **Documentation table** (line ~118): Add entries for the 5 new v3.0 feature pages:
       - Session Recall, Polaris, Contradiction Detection, Media Ingestion, Methodology

    5. **Architecture section** (line ~136): Update the package tree to include `controllers/` and `actions/` directories, and add a brief note about the ActionRegistry pattern.

    Preserve all existing content that is still accurate. Do NOT remove working links or badges.
  </action>
  <verify>
    <automated>grep -c "session recall" README.md && grep -c "polaris" README.md && grep -c "contradiction" README.md && grep -c "media ingestion" README.md && grep -c "controllers/" README.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -i "session recall" README.md returns at least 1 match
    - grep -i "polaris" README.md returns at least 1 match
    - grep -i "contradiction" README.md returns at least 1 match
    - grep -i "media ingestion" README.md returns at least 1 match (case insensitive)
    - grep "controllers/" README.md returns at least 1 match
    - grep "actions/" README.md returns at least 1 match
    - grep "recall-topic\|recall-temporal\|recall-topology" README.md returns at least 1 match
    - All existing documentation links still present and valid
  </acceptance_criteria>
  <done>README.md features list, quick start, documentation table, and architecture section all reflect v3.0 capabilities including session recall, polaris, contradiction detection, and media ingestion</done>
</task>

</tasks>

<verification>
- CLAUDE.md architecture section contains accurate service/controller/action counts verified against source
- README.md features list covers all v3.0 features
- No sections of either file outside the target areas were modified
- Both files pass markdownlint basic structure
</verification>

<success_criteria>
- IDOC-01 satisfied: CLAUDE.md architecture section updated with v3.0 services, controllers, actions, feature-local registration, centralized PM
- IDOC-03 satisfied: README.md feature list and command examples updated for v3.0
- All counts verified against actual source code
</success_criteria>

<output>
After completion, create `.planning/phases/27-internal-documentation-refresh/27-01-SUMMARY.md`
</output>
