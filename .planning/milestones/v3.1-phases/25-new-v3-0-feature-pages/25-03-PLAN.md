---
phase: 25-new-v3-0-feature-pages
plan: 03
type: execute
wave: 2
depends_on:
  - 25-01
  - 25-02
files_modified:
  - docs/methodology.md
  - mkdocs.yml
  - docs/llms.txt
  - docs/llms-full.txt
autonomous: true
requirements:
  - NDOC-05

must_haves:
  truths:
    - "User can read a methodology.md page covering prose-as-title convention, title quality check severity, and garden backlog candidates"
    - "Each new page has a mkdocs.yml nav entry replacing the Phase 25 placeholder comment"
    - "Each new page has an llms.txt entry and llms-full.txt append"
    - "mkdocs build --strict passes with all 5 new pages wired into nav"
  artifacts:
    - path: "docs/methodology.md"
      provides: "Methodology guidance documentation"
      contains: "prose-as-title"
    - path: "mkdocs.yml"
      provides: "Navigation entries for all 5 new pages"
      contains: "session-recall.md"
    - path: "docs/llms.txt"
      provides: "Agent discovery index with new page entries"
      contains: "session-recall"
    - path: "docs/llms-full.txt"
      provides: "Full agent discovery index with new page content summaries"
      contains: "polaris"
  key_links:
    - from: "mkdocs.yml"
      to: "docs/session-recall.md"
      via: "nav entry replaces placeholder comment"
      pattern: "Session recall.*session-recall\\.md"
    - from: "docs/llms.txt"
      to: "docs/"
      via: "URL entries for all 5 new pages"
      pattern: "session-recall|polaris|contradiction|media-ingestion|methodology"
---

<objective>
Write the methodology.md page, wire all 5 new pages into mkdocs.yml navigation (replacing Phase 25 placeholder comments), and update llms.txt + llms-full.txt agent discovery indexes.

Purpose: Complete Phase 25 by writing the final page and ensuring all 5 pages are navigable in the docs site and discoverable by agents via llms.txt indexes. This plan runs after Plans 01 and 02 so all pages exist before wiring.
Output: methodology.md page, updated mkdocs.yml nav, updated llms.txt and llms-full.txt.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-new-v3-0-feature-pages/25-CONTEXT.md
@.planning/phases/25-new-v3-0-feature-pages/25-01-SUMMARY.md
@.planning/phases/25-new-v3-0-feature-pages/25-02-SUMMARY.md
@CLAUDE.md (Documentation Rules and Documentation Conventions sections)
@mkdocs.yml (current nav with Phase 25 placeholder comments)
@docs/llms.txt (current agent discovery index)
@docs/llms-full.txt (current full agent discovery index)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write methodology.md (NDOC-05)</name>
  <files>docs/methodology.md</files>
  <read_first>
    - src/ztlctl/services/check.py (title quality check — look for title_quality, short_title, generic_title, CAT_STRUCTURAL)
    - src/ztlctl/templates/self/methodology.md.j2 (methodology template — prose-as-title section and examples)
    - src/ztlctl/services/init.py (how methodology.md.j2 is scaffolded during init)
    - src/ztlctl/mcp/resources.py (garden_backlog resource — title improvement candidates)
    - CLAUDE.md (Documentation Conventions)
  </read_first>
  <action>
Create docs/methodology.md as a How-to guide (per Diataxis).

Run `uv run ztlctl check --help` to verify flags related to title quality checks. Verify severity levels and category names against source.

Page structure:

1. **Frontmatter**: `title: Methodology guidance`
2. **H1: Methodology guidance** — 2-3 sentence overview: ztlctl encodes Zettelkasten methodology conventions that the integrity scanner enforces and the init templates teach
3. **H2: Prose-as-title convention** — what it means (titles should be complete thoughts, not keywords), why it matters (titles carry the idea in graph views), concrete good/bad examples (from methodology.md.j2 template)
4. **H2: Title quality checks** — how `ztlctl check` flags short or generic titles under CAT_STRUCTURAL at info severity. What "short" and "generic" mean (thresholds from source). Example check output.
5. **H2: Garden backlog candidates** — how the garden backlog resource includes title improvement candidates alongside stale seeds and orphans. How agents and users can use this to systematically improve titles.
6. **H2: Init template** — the methodology.md.j2 template scaffolded by `ztlctl init` — what it contains, where it lives, how to customize
7. **H2: What's next** — links to concepts.md, best-practices.md, tutorial.md

Admonitions: use only warning/note/tip. Keep page focused — this is methodology guidance, not a full Zettelkasten tutorial.
  </action>
  <verify>
    <automated>test -f docs/methodology.md && grep -q "prose-as-title\|Prose-as-title" docs/methodology.md && grep -q "title quality\|title_quality" docs/methodology.md && grep -qi "garden backlog\|garden_backlog" docs/methodology.md && grep -q "What's next" docs/methodology.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at docs/methodology.md
    - Contains frontmatter with `title: Methodology guidance`
    - Documents prose-as-title convention with good/bad examples
    - Documents title quality check severity (info) and category (CAT_STRUCTURAL)
    - Documents garden backlog title improvement candidates
    - References methodology.md.j2 init template
    - Ends with "What's next" section with 2-3 relative links
    - Uses only warning/note/tip admonitions
    - Headings are sentence case
  </acceptance_criteria>
  <done>methodology.md is a complete How-to page covering prose-as-title convention, title quality checks, garden backlog, and init template with verified details and cross-references</done>
</task>

<task type="auto">
  <name>Task 2: Wire all 5 pages into mkdocs.yml nav and update llms.txt + llms-full.txt</name>
  <files>mkdocs.yml, docs/llms.txt, docs/llms-full.txt</files>
  <read_first>
    - mkdocs.yml (current nav — find the 5 Phase 25 placeholder comments)
    - docs/llms.txt (current entries — understand format and ordering)
    - docs/llms-full.txt (current entries — understand format for multi-line entries)
    - docs/session-recall.md (verify exists, read title from frontmatter)
    - docs/polaris.md (verify exists, read title)
    - docs/contradiction-detection.md (verify exists, read title)
    - docs/media-ingestion.md (verify exists, read title)
    - docs/methodology.md (verify exists, read title)
  </read_first>
  <action>
**mkdocs.yml nav update:**

Replace the 5 Phase 25 placeholder comments in mkdocs.yml nav (between Configuration and Built-in Plugins) with actual nav entries. The entries should use the page titles from frontmatter:

```yaml
    - Session Recall: session-recall.md
    - Polaris Priorities: polaris.md
    - Contradiction Detection: contradiction-detection.md
    - Media Ingestion: media-ingestion.md
    - Methodology Guidance: methodology.md
```

Remove the comment lines (`# session-recall.md — Phase 25`, etc.) and replace with the above entries. Preserve all other nav structure.

**llms.txt update:**

Add 5 new entries to the User Guide section of docs/llms.txt, positioned between existing entries (after Configuration, before Built-in Plugins — matching nav order):

```
- [Session Recall](https://thatdevstudio.github.io/ztlctl/session-recall/): Query session history by time, topic, or connectivity
- [Polaris Priorities](https://thatdevstudio.github.io/ztlctl/polaris/): Strategic priorities layer for agent alignment
- [Contradiction Detection](https://thatdevstudio.github.io/ztlctl/contradiction-detection/): Surface and confirm contradicting notes in the vault
- [Media Ingestion](https://thatdevstudio.github.io/ztlctl/media-ingestion/): Ingest audio, video, and transcripts as captured references
- [Methodology Guidance](https://thatdevstudio.github.io/ztlctl/methodology/): Prose-as-title convention and title quality checks
```

**llms-full.txt update:**

Append multi-line entries for all 5 new pages to docs/llms-full.txt. Each entry should include: page title, URL, and a 2-3 sentence summary of what the page covers. Follow the existing format in the file.

**Verification build:**

Run `uv run mkdocs build --strict` to verify all nav entries resolve and the build passes.
  </action>
  <verify>
    <automated>grep -q "session-recall.md" mkdocs.yml && grep -q "polaris.md" mkdocs.yml && grep -q "contradiction-detection.md" mkdocs.yml && grep -q "media-ingestion.md" mkdocs.yml && grep -q "methodology.md" mkdocs.yml && ! grep -q "# session-recall.md" mkdocs.yml && grep -q "session-recall" docs/llms.txt && grep -q "polaris" docs/llms.txt && grep -q "contradiction" docs/llms.txt && grep -q "media-ingestion" docs/llms.txt && grep -q "methodology" docs/llms.txt && grep -q "session-recall" docs/llms-full.txt && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - mkdocs.yml nav contains entries for all 5 new pages (no placeholder comments remain)
    - Nav entries are positioned between Configuration and Built-in Plugins
    - docs/llms.txt contains entries for all 5 new pages with descriptions
    - docs/llms-full.txt contains multi-line entries for all 5 new pages
    - `uv run mkdocs build --strict` passes without errors
    - No Phase 25 placeholder comments remain in mkdocs.yml
  </acceptance_criteria>
  <done>All 5 new pages are wired into mkdocs.yml nav (placeholders replaced), llms.txt has entries for all 5, llms-full.txt has entries for all 5, and mkdocs build --strict passes</done>
</task>

</tasks>

<verification>
1. `uv run mkdocs build --strict` passes — all 5 new pages resolve in nav
2. All 5 Phase 25 placeholder comments removed from mkdocs.yml
3. llms.txt has 5 new entries in User Guide section
4. llms-full.txt has 5 new multi-line entries
5. methodology.md covers NDOC-05 requirements
</verification>

<success_criteria>
- docs/methodology.md covers NDOC-05 requirements completely
- mkdocs.yml nav has entries for all 5 new pages (session-recall, polaris, contradiction-detection, media-ingestion, methodology)
- No Phase 25 placeholder comments remain in mkdocs.yml
- llms.txt and llms-full.txt updated with all 5 new pages
- `uv run mkdocs build --strict` passes
</success_criteria>

<output>
After completion, create `.planning/phases/25-new-v3-0-feature-pages/25-03-SUMMARY.md`
</output>
