---
phase: 25-new-v3-0-feature-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/session-recall.md
  - docs/polaris.md
autonomous: true
requirements:
  - NDOC-01
  - NDOC-02

must_haves:
  truths:
    - "User can read a session-recall.md page covering temporal, topic, and topology recall with CLI usage, MCP tools, and agent workflow examples"
    - "User can read a polaris.md page covering init scaffold, MCP resource, context assembly, check_alignment action, and agent alignment workflow"
  artifacts:
    - path: "docs/session-recall.md"
      provides: "Session recall feature documentation"
      contains: "recall_temporal"
    - path: "docs/polaris.md"
      provides: "Polaris priorities layer documentation"
      contains: "check_alignment"
  key_links:
    - from: "docs/session-recall.md"
      to: "src/ztlctl/services/recall.py"
      via: "CLI flags and MCP tool names verified against source"
      pattern: "recall_temporal|recall_topic|recall_topology"
    - from: "docs/polaris.md"
      to: "src/ztlctl/services/context.py"
      via: "MCP resource and check_alignment verified against source"
      pattern: "check_alignment|ztlctl://polaris"
---

<objective>
Write two standalone v3.0 feature documentation pages: session-recall.md and polaris.md.

Purpose: Users and agents need discoverable documentation for session recall (temporal/topic/topology queries) and polaris (strategic priorities layer). These are the two most interconnected v3.0 features.
Output: Two complete docs pages in docs/ following Documentation Conventions from CLAUDE.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-new-v3-0-feature-pages/25-CONTEXT.md
@CLAUDE.md (Documentation Rules and Documentation Conventions sections)
@docs/plugins.md (established page pattern: frontmatter, overview, sections, config, what's next)
@mkdocs.yml (nav structure — pages go in docs/ root)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write session-recall.md (NDOC-01)</name>
  <files>docs/session-recall.md</files>
  <read_first>
    - src/ztlctl/services/recall.py (RecallService — all public methods, parameters, return types)
    - src/ztlctl/controllers/recall.py (RecallController — CLI command wiring)
    - src/ztlctl/actions/_session.py (recall_temporal, recall_topic, recall_topology ActionDefinitions)
    - src/ztlctl/mcp/resources.py (ztlctl://sessions/recent resource definition)
    - docs/plugins.md (established page structure pattern)
    - CLAUDE.md (Documentation Conventions section — CLI syntax, admonitions, headings, cross-refs)
  </read_first>
  <action>
Create docs/session-recall.md as a How-to guide (per Diataxis). Verify every CLI flag and parameter name against source before writing.

Run `uv run ztlctl recall --help` (and subcommands) to capture exact CLI interface.

Page structure (sentence-case headings, one H1):

1. **Frontmatter**: `title: Session recall`
2. **H1: Session recall** — 2-3 sentence overview: query session history by time, topic, or connectivity
3. **H2: Temporal recall** — date-range queries, CLI usage with exact flags from --help, example output
4. **H2: Topic recall** — text search across sessions, CLI usage, example
5. **H2: Topology recall** — session connectivity and shared content, CLI usage, example
6. **H2: MCP tools** — table of recall_temporal, recall_topic, recall_topology tools with parameters (from ActionDefinitions), plus ztlctl://sessions/recent resource
7. **H2: Agent workflow example** — concrete example: agent uses temporal recall to load recent context, then topic recall to find related sessions
8. **H2: Configuration** — any relevant config options (check ZtlSettings for recall-related fields)
9. **H2: What's next** — links to concepts.md, agentic-workflows.md, commands.md

Admonitions: use only warning/note/tip. CLI examples use `$ ztlctl` prefix in code blocks. Cross-references use relative markdown links.
  </action>
  <verify>
    <automated>test -f docs/session-recall.md && grep -q "recall_temporal" docs/session-recall.md && grep -q "recall_topic" docs/session-recall.md && grep -q "recall_topology" docs/session-recall.md && grep -q "ztlctl://sessions/recent" docs/session-recall.md && grep -q "What's next" docs/session-recall.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at docs/session-recall.md
    - Contains frontmatter with `title: Session recall`
    - Documents all three recall modes: temporal, topic, topology
    - CLI flag names match `uv run ztlctl recall --help` output exactly
    - MCP tool names match ActionDefinition names in _session.py
    - Contains ztlctl://sessions/recent resource reference
    - Has agent workflow example section
    - Ends with "What's next" section with 2-3 relative links
    - Uses only warning/note/tip admonitions
    - Headings are sentence case
  </acceptance_criteria>
  <done>session-recall.md is a complete How-to page covering all three recall modes with verified CLI flags, MCP tools, agent workflow, and cross-references</done>
</task>

<task type="auto">
  <name>Task 2: Write polaris.md (NDOC-02)</name>
  <files>docs/polaris.md</files>
  <read_first>
    - src/ztlctl/services/context.py (ContextService — polaris integration, check_alignment)
    - src/ztlctl/services/contracts.py (PolarisContext or similar contracts)
    - src/ztlctl/services/check.py (check_alignment method if present)
    - src/ztlctl/actions/_check.py (check_alignment ActionDefinition)
    - src/ztlctl/mcp/resources.py (ztlctl://polaris resource definition)
    - src/ztlctl/templates/self/polaris.md.j2 (init scaffold template)
    - src/ztlctl/services/init.py (polaris scaffold in init flow)
    - CLAUDE.md (Documentation Conventions)
  </read_first>
  <action>
Create docs/polaris.md as a How-to guide (per Diataxis). Frame polaris as the "strategic layer" of the vault per CONTEXT.md.

Run `uv run ztlctl check --help` to find check_alignment flags. Verify all parameter names against source.

Page structure:

1. **Frontmatter**: `title: Polaris priorities`
2. **H1: Polaris priorities** — 2-3 sentence overview: polaris is the strategic priorities layer; a persistent document that agents consult for alignment
3. **H2: The polaris document** — what it is, where it lives (garden/groves/polaris.md), how init scaffolds it from polaris.md.j2, what to put in it
4. **H2: Init scaffold** — `ztlctl init` creates polaris.md automatically, show the template structure
5. **H2: MCP resource** — ztlctl://polaris resource: what it returns, how agents consume it in Layer 1 of context assembly, token budgeting
6. **H2: Context assembly integration** — how polaris content flows into context assembly (from ContextService), which layer it occupies
7. **H2: Alignment checking** — check_alignment action: CLI usage with exact flags, what it accepts (decision description), what it returns (structured polaris context)
8. **H2: Agent decision workflow** — concrete example: agent reads polaris resource, makes a decision, checks alignment before executing
9. **H2: What's next** — links to agentic-workflows.md, configuration.md, concepts.md

Admonitions: use only warning/note/tip. Frame as strategic — this is the "why" layer that guides agent behavior.
  </action>
  <verify>
    <automated>test -f docs/polaris.md && grep -q "check_alignment" docs/polaris.md && grep -q "ztlctl://polaris" docs/polaris.md && grep -q "polaris.md.j2\|polaris\.md" docs/polaris.md && grep -q "What's next" docs/polaris.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at docs/polaris.md
    - Contains frontmatter with `title: Polaris priorities`
    - Describes polaris as the strategic layer of the vault
    - Documents init scaffold (polaris.md.j2 template)
    - Documents ztlctl://polaris MCP resource
    - Documents context assembly integration (Layer 1)
    - Documents check_alignment action with verified CLI flags
    - Has agent decision workflow example
    - Ends with "What's next" section with 2-3 relative links
    - Uses only warning/note/tip admonitions
    - Headings are sentence case
  </acceptance_criteria>
  <done>polaris.md is a complete How-to page covering the strategic priorities layer with verified CLI flags, MCP resource, context assembly, agent workflow, and cross-references</done>
</task>

</tasks>

<verification>
Both pages exist in docs/ with correct frontmatter, verified CLI flags, MCP tool/resource references, agent workflow examples, and "What's next" cross-references. All headings are sentence case. Only warning/note/tip admonitions used.
</verification>

<success_criteria>
- docs/session-recall.md covers NDOC-01 requirements completely
- docs/polaris.md covers NDOC-02 requirements completely
- Every CLI flag name verified against `uv run ztlctl <command> --help`
- Every MCP tool/resource name verified against ActionDefinitions and resources.py
- Pages follow Documentation Conventions from CLAUDE.md
</success_criteria>

<output>
After completion, create `.planning/phases/25-new-v3-0-feature-pages/25-01-SUMMARY.md`
</output>
