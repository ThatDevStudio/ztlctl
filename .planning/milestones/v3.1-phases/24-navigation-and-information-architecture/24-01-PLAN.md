---
phase: 24-navigation-and-information-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mkdocs.yml
  - CLAUDE.md
  - .planning/phases/24-navigation-and-information-architecture/24-DIATAXIS-AUDIT.md
autonomous: true
requirements:
  - QUAL-01
  - QUAL-04

must_haves:
  truths:
    - "Every existing docs page has a recorded Diataxis classification in the audit artifact"
    - "The User Guide nav order in mkdocs.yml follows install -> quickstart -> tutorial -> concepts -> paradigms -> commands -> config -> [5 feature page slots] -> plugins -> obsidian -> agentic-workflows -> best-practices -> troubleshooting"
    - "Placeholder comment markers exist for all 5 v3.0 feature pages in mkdocs.yml nav"
    - "CLAUDE.md contains CLI syntax, admonition taxonomy, and cross-referencing conventions"
  artifacts:
    - path: ".planning/phases/24-navigation-and-information-architecture/24-DIATAXIS-AUDIT.md"
      provides: "Diataxis classification of all existing docs pages"
      contains: "tutorial"
    - path: "mkdocs.yml"
      provides: "Reordered nav with placeholder slots"
      contains: "session-recall"
    - path: "CLAUDE.md"
      provides: "Documentation Conventions subsection"
      contains: "Documentation Conventions"
  key_links:
    - from: "24-DIATAXIS-AUDIT.md"
      to: "mkdocs.yml"
      via: "audit informs nav ordering"
      pattern: "tutorial.*how-to.*reference.*explanation"
    - from: "CLAUDE.md"
      to: "docs/*.md"
      via: "conventions enforced by writers"
      pattern: "admonition taxonomy"
---

<objective>
Audit all existing docs pages for Diataxis classification, reorder the User Guide navigation for beginner-to-advanced progression, insert placeholder slots for 5 new v3.0 feature pages, and document quality conventions in CLAUDE.md.

Purpose: Establish navigation structure and quality standards before Phase 25 writes new pages.
Output: Diataxis audit artifact, reordered mkdocs.yml nav, CLAUDE.md conventions section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-navigation-and-information-architecture/24-CONTEXT.md

<interfaces>
<!-- Current mkdocs.yml nav structure (to be reordered) -->
From mkdocs.yml:
```yaml
nav:
  - Home: index.md
  - Installation: installation.md
  - Quick Start: quickstart.md
  - User Guide:
    - guide/index.md
    - Tutorial: tutorial.md
    - Core Concepts: concepts.md
    - Knowledge Paradigms: paradigms.md
    - Obsidian Starter Kit: obsidian.md
    - Built-in Plugins: plugins.md
    - Agentic Workflows: agentic-workflows.md
    - Command Reference: commands.md
    - Configuration: configuration.md
    - Troubleshooting: troubleshooting.md
    - Best Practices: best-practices.md
  - Developer Guide:
    - dev/index.md
    - Contributing: development.md
    - Plugin Authoring: plugin-guide.md
    - API Reference: api-reference.md
    - MCP Server: mcp.md
    - Agent System Manual: agents.md
```

<!-- Current CLAUDE.md Documentation Rules section (to be extended) -->
From CLAUDE.md (line 147-168):
```markdown
## Documentation Rules

**Rule:** If a PR adds or modifies actions, commands, config options, or MCP resources, the same PR MUST update the relevant docs.

### Docs Update Checklist
[...]

### GSD Phase Documentation Convention (DINF-03)
[...]

## Architecture
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diataxis audit and mkdocs.yml nav reorder (QUAL-01)</name>
  <files>
    mkdocs.yml,
    .planning/phases/24-navigation-and-information-architecture/24-DIATAXIS-AUDIT.md
  </files>
  <read_first>
    mkdocs.yml,
    docs/index.md,
    docs/installation.md,
    docs/quickstart.md,
    docs/tutorial.md,
    docs/concepts.md,
    docs/paradigms.md,
    docs/commands.md,
    docs/configuration.md,
    docs/plugins.md,
    docs/obsidian.md,
    docs/agentic-workflows.md,
    docs/best-practices.md,
    docs/troubleshooting.md,
    docs/guide/index.md,
    docs/dev/index.md,
    docs/development.md,
    docs/plugin-guide.md,
    docs/api-reference.md,
    docs/mcp.md,
    docs/agents.md
  </read_first>
  <action>
**Step 1: Create Diataxis audit artifact.**

Create `.planning/phases/24-navigation-and-information-architecture/24-DIATAXIS-AUDIT.md` with a table classifying every existing docs page. Read each page (listed in read_first) and classify by Diataxis type. Use these expected classifications (per D-QUAL-01 user decisions) but verify by reading each page:

| Page | Diataxis Type | Notes |
|------|--------------|-------|
| index.md | Landing | Not a content page |
| installation.md | How-to | Step-by-step install |
| quickstart.md | Tutorial | Guided first experience |
| tutorial.md | Tutorial | Extended learning path |
| concepts.md | Explanation | Theory and mental models |
| paradigms.md | Explanation | Knowledge paradigm theory |
| commands.md | Reference | CLI command catalog |
| configuration.md | Reference | Config option catalog |
| plugins.md | Reference | Plugin listing |
| obsidian.md | How-to | Integration setup guide |
| agentic-workflows.md | How-to | Workflow recipes |
| best-practices.md | How-to / Explanation | Flag if mixed |
| troubleshooting.md | How-to | Problem-solution pairs |
| guide/index.md | Landing | Section index |
| dev/index.md | Landing | Section index |
| development.md | How-to | Contributor setup |
| plugin-guide.md | How-to | Plugin authoring guide |
| api-reference.md | Reference | Python API docs |
| mcp.md | Reference | MCP server docs |
| agents.md | Reference | Agent system docs |

For mixed-purpose pages (e.g., best-practices.md), add a "Remediation" column noting what should be done in Phase 26. Include a summary section at the top with counts by type: N tutorials, N how-tos, N references, N explanations.

**Step 2: Reorder mkdocs.yml nav.**

Replace the `nav:` section in mkdocs.yml with the following reordered structure. The User Guide follows beginner-to-advanced progression per the locked decision. Developer Guide order is unchanged. Add comment markers for the 5 placeholder slots:

```yaml
nav:
  - Home: index.md
  - Installation: installation.md
  - Quick Start: quickstart.md
  - User Guide:
    - guide/index.md
    - Tutorial: tutorial.md
    - Core Concepts: concepts.md
    - Knowledge Paradigms: paradigms.md
    - Command Reference: commands.md
    - Configuration: configuration.md
    # session-recall.md — Phase 25
    # polaris.md — Phase 25
    # contradiction-detection.md — Phase 25
    # media-ingestion.md — Phase 25
    # methodology.md — Phase 25
    - Built-in Plugins: plugins.md
    - Obsidian Starter Kit: obsidian.md
    - Agentic Workflows: agentic-workflows.md
    - Best Practices: best-practices.md
    - Troubleshooting: troubleshooting.md
  - Developer Guide:
    - dev/index.md
    - Contributing: development.md
    - Plugin Authoring: plugin-guide.md
    - API Reference: api-reference.md
    - MCP Server: mcp.md
    - Agent System Manual: agents.md
```

Key changes from current nav:
- Commands and Configuration moved up (before feature pages) — reference material the user needs before deep-dives
- Obsidian moved down (after plugins) — extensibility section
- 5 placeholder comment slots inserted between Configuration and Built-in Plugins
- Best Practices moved after Agentic Workflows (near end, advanced topic)

Preserve ALL other mkdocs.yml content (theme, plugins, markdown_extensions, etc.) exactly as-is. Only modify the `nav:` block.

After writing, run `uv run mkdocs build --strict` from the project root to verify the build still passes with the reordered nav.
  </action>
  <verify>
    <automated>cd /Users/shparki/Documents/Workspace/thatdev/ztlctl && uv run mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/24-navigation-and-information-architecture/24-DIATAXIS-AUDIT.md` exists and contains a table with at least 17 rows (one per docs page)
    - Audit file contains the words "tutorial", "how-to", "reference", "explanation" as classification values
    - mkdocs.yml nav contains "Command Reference: commands.md" BEFORE the placeholder comments
    - mkdocs.yml nav contains "# session-recall.md" comment marker
    - mkdocs.yml nav contains "# polaris.md" comment marker
    - mkdocs.yml nav contains "# contradiction-detection.md" comment marker
    - mkdocs.yml nav contains "# media-ingestion.md" comment marker
    - mkdocs.yml nav contains "# methodology.md" comment marker
    - mkdocs.yml nav contains "Built-in Plugins: plugins.md" AFTER the placeholder comments
    - mkdocs.yml nav contains "Obsidian Starter Kit: obsidian.md" AFTER "Built-in Plugins: plugins.md"
    - `uv run mkdocs build --strict` exits 0
  </acceptance_criteria>
  <done>All existing docs pages classified by Diataxis type in audit artifact. mkdocs.yml nav reordered to beginner-to-advanced progression with 5 placeholder slots for v3.0 feature pages. MkDocs build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Document quality conventions in CLAUDE.md (QUAL-04)</name>
  <files>CLAUDE.md</files>
  <read_first>CLAUDE.md</read_first>
  <action>
Add a new `### Documentation Conventions` subsection inside the existing `## Documentation Rules` section in CLAUDE.md. Insert it AFTER the `### GSD Phase Documentation Convention (DINF-03)` subsection and BEFORE the `## Architecture` section.

The new subsection must contain exactly these conventions (per locked decisions):

```markdown
### Documentation Conventions

Quality standards for all docs pages. Enforced by review — Vale and pymarkdownlnt catch structural violations.

**CLI syntax (Google developer style):**
- Optional arguments: `[--flag VALUE]`
- Required arguments: `REQUIRED`
- Shell prompts: use bare `ztlctl` in inline references, `$ ztlctl` only in standalone code blocks
- Flag names must match `uv run ztlctl <command> --help` output exactly — never from memory

**Admonition taxonomy (3 types only):**
- `!!! warning` — danger, breaking changes, data loss risks
- `!!! note` — context, background information, important caveats
- `!!! tip` — recommendations, best practices, efficiency suggestions

Do not use `info`, `danger`, `example`, `abstract`, or other admonition types. Three types keep the signal clear.

**Cross-referencing:**
- Every page ends with a "What's next" section containing 2-3 links to related pages
- Use relative Markdown links: `[Page title](page.md)` (not absolute URLs)
- Link text should be the target page's title, not "click here" or "see this"

**Headings:**
- Sentence case (e.g., "Getting started" not "Getting Started") — consistent with Vale Google style
- H1 is the page title (one per page), H2 for major sections, H3 for subsections

**Diataxis content types:**
- Tutorial: learning-oriented, guided experience ("Follow along to...")
- How-to: task-oriented, goal-driven ("How to configure...")
- Reference: information-oriented, complete catalog ("All available commands...")
- Explanation: understanding-oriented, conceptual ("Why ztlctl uses...")
- Do not mix types in a single page — if a page serves two purposes, split or pick the primary purpose
```

Do NOT modify any other section of CLAUDE.md. The `## Architecture` section must remain immediately after the new subsection.
  </action>
  <verify>
    <automated>cd /Users/shparki/Documents/Workspace/thatdev/ztlctl && grep -c "Documentation Conventions" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains `### Documentation Conventions` heading
    - CLAUDE.md contains the string `[--flag VALUE]` (CLI syntax convention)
    - CLAUDE.md contains the string `!!! warning` (admonition taxonomy)
    - CLAUDE.md contains the string `!!! note` (admonition taxonomy)
    - CLAUDE.md contains the string `!!! tip` (admonition taxonomy)
    - CLAUDE.md contains the string "What's next" (cross-referencing convention)
    - CLAUDE.md contains the string "Sentence case" (heading convention)
    - CLAUDE.md contains the string "Diataxis content types" (type definitions)
    - The `### Documentation Conventions` section appears AFTER `### GSD Phase Documentation Convention` and BEFORE `## Architecture`
    - No other sections of CLAUDE.md are modified (git diff shows changes only in the Documentation Rules area)
  </acceptance_criteria>
  <done>CLAUDE.md contains Documentation Conventions subsection with CLI syntax, admonition taxonomy, cross-referencing, heading, and Diataxis type conventions. All future docs work follows these standards.</done>
</task>

</tasks>

<verification>
1. `uv run mkdocs build --strict` passes (nav reorder did not break build)
2. `24-DIATAXIS-AUDIT.md` exists with classifications for all docs pages
3. `mkdocs.yml` nav follows beginner-to-advanced order with 5 placeholder slots
4. `CLAUDE.md` has Documentation Conventions with all 5 convention areas
</verification>

<success_criteria>
- Every existing docs page has a Diataxis classification recorded in the audit artifact
- User Guide nav order reflects beginner-to-advanced progression
- 5 placeholder slots exist in nav for v3.0 feature pages
- Quality conventions documented in CLAUDE.md cover CLI syntax, admonitions, cross-refs, headings, and Diataxis types
- MkDocs build passes with reordered nav
</success_criteria>

<output>
After completion, create `.planning/phases/24-navigation-and-information-architecture/24-01-SUMMARY.md`
</output>
