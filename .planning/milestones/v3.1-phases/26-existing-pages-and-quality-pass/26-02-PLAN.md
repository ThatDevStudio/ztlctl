---
phase: 26-existing-pages-and-quality-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/llms.txt
  - docs/llms-full.txt
autonomous: true
requirements: [QUAL-03]

must_haves:
  truths:
    - "llms.txt contains entries for all new v3.0 feature pages with accurate descriptions"
    - "llms-full.txt contains full content sections for all new v3.0 feature pages"
    - "Existing page descriptions in both files reflect v3.0 reality (updated tool counts, new features)"
    - "An agent using llms.txt discovers session recall, polaris, contradiction detection, media ingestion, and methodology"
  artifacts:
    - path: "docs/llms.txt"
      provides: "Agent discovery index with v3.0 entries"
      contains: "session-recall"
    - path: "docs/llms-full.txt"
      provides: "Full agent discovery corpus with v3.0 content"
      contains: "polaris"
  key_links:
    - from: "docs/llms.txt"
      to: "docs/session-recall.md"
      via: "URL entry"
      pattern: "session-recall"
    - from: "docs/llms.txt"
      to: "docs/polaris.md"
      via: "URL entry"
      pattern: "polaris"
    - from: "docs/llms.txt"
      to: "docs/contradiction-detection.md"
      via: "URL entry"
      pattern: "contradiction-detection"
    - from: "docs/llms.txt"
      to: "docs/media-ingestion.md"
      via: "URL entry"
      pattern: "media-ingestion"
    - from: "docs/llms.txt"
      to: "docs/methodology.md"
      via: "URL entry"
      pattern: "methodology"
---

<objective>
Refresh llms.txt and llms-full.txt to ensure all v3.0 feature pages are listed and existing page descriptions are accurate.

Purpose: Agent discovery indexes must be current for agents to find v3.0 features (session recall, polaris, contradiction detection, media ingestion, methodology). Phase 25 added the new pages to these files, but existing page descriptions (e.g., mcp.md tool count, agents.md action count) may be stale.
Output: Both llms.txt and llms-full.txt with verified, current content.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/llms.txt
@docs/llms-full.txt
@docs/session-recall.md
@docs/polaris.md
@docs/contradiction-detection.md
@docs/media-ingestion.md
@docs/methodology.md
@docs/concepts.md
@docs/agentic-workflows.md
@docs/agents.md
@docs/mcp.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and update llms.txt entries</name>
  <files>docs/llms.txt</files>
  <read_first>
    docs/llms.txt
    docs/session-recall.md
    docs/polaris.md
    docs/contradiction-detection.md
    docs/media-ingestion.md
    docs/methodology.md
  </read_first>
  <action>
    Read docs/llms.txt fully. Check for:

    1. **New page entries exist** — Verify that all 5 v3.0 feature pages are listed:
       - Session Recall (session-recall.md)
       - Polaris Priorities (polaris.md)
       - Contradiction Detection (contradiction-detection.md)
       - Media Ingestion (media-ingestion.md)
       - Methodology Guidance (methodology.md)
       If any are missing, add them in the User Guide section following the existing format (URL + one-line description).

    2. **Existing descriptions are accurate** — Check and update descriptions for:
       - MCP Server entry: should mention 73+ tools (not 59), 20 resources (not 17)
       - Agent System Manual entry: should mention 73+ actions
       - Landing page / index entry: should mention 73+ MCP tools (not 59)
       - Agentic Workflows entry: should mention v3.0 recipes if the description is feature-specific

    3. **Description format** — Each entry should follow the existing pattern:
       ```
       - [Page Title](URL): One-line description
       ```

    Do NOT rewrite the entire file. Only update stale numbers and add missing entries.
  </action>
  <verify>
    <automated>grep -c "session-recall" docs/llms.txt && grep -c "polaris" docs/llms.txt && grep -c "contradiction-detection" docs/llms.txt && grep -c "media-ingestion" docs/llms.txt && grep -c "methodology" docs/llms.txt</automated>
  </verify>
  <acceptance_criteria>
    - grep "session-recall" docs/llms.txt returns >= 1 match
    - grep "polaris" docs/llms.txt returns >= 1 match
    - grep "contradiction-detection" docs/llms.txt returns >= 1 match
    - grep "media-ingestion" docs/llms.txt returns >= 1 match
    - grep "methodology" docs/llms.txt returns >= 1 match
    - grep "73" docs/llms.txt returns >= 1 match (updated tool count in relevant descriptions)
  </acceptance_criteria>
  <done>llms.txt contains entries for all 5 v3.0 feature pages and existing descriptions reflect current tool/resource counts.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and update llms-full.txt entries</name>
  <files>docs/llms-full.txt</files>
  <read_first>
    docs/llms-full.txt
    docs/session-recall.md
    docs/polaris.md
    docs/contradiction-detection.md
    docs/media-ingestion.md
    docs/methodology.md
  </read_first>
  <action>
    Read docs/llms-full.txt fully. This file contains the full content of each docs page as a concatenated corpus. Check for:

    1. **New page sections exist** — Verify that all 5 v3.0 feature pages have full content sections with URL blockquote headers (per the Phase 25 decision: "llms-full.txt section headers include URL blockquote lines so agent grep patterns resolve correctly"). If any are missing, append them following the established pattern:
       ```
       # Page Title
       > URL: https://thatdevstudio.github.io/ztlctl/page-slug/

       [full page content]
       ```

    2. **Existing page descriptions are accurate** — Check the following sections for stale content:
       - The landing/index section: tool count should be 73+ (not 59)
       - The MCP Server section: tool count should be 73+, resource count should be 20
       - The Agent System Manual section: action count should reflect v3.0 additions
       Update any stale numbers to match v3.0 reality.

    3. **Do NOT rewrite full page content sections** — Only fix stale numbers in existing sections and ensure new page sections are present. The full page content for new pages should be copied from the actual .md files.

    IMPORTANT: llms-full.txt can be very large. Read it in sections if needed. Focus on verifying presence of the 5 new sections and fixing stale numbers in existing sections.
  </action>
  <verify>
    <automated>grep -c "Session Recall" docs/llms-full.txt && grep -c "Polaris" docs/llms-full.txt && grep -c "Contradiction Detection" docs/llms-full.txt && grep -c "Media Ingestion" docs/llms-full.txt && grep -c "Methodology" docs/llms-full.txt && grep -c "73" docs/llms-full.txt</automated>
  </verify>
  <acceptance_criteria>
    - grep "Session Recall" docs/llms-full.txt returns >= 1 match
    - grep "Polaris" docs/llms-full.txt returns >= 1 match
    - grep "Contradiction Detection" docs/llms-full.txt returns >= 1 match
    - grep "Media Ingestion" docs/llms-full.txt returns >= 1 match
    - grep "Methodology" docs/llms-full.txt returns >= 1 match
    - grep "73" docs/llms-full.txt returns >= 1 match (updated tool count)
    - grep "thatdevstudio.github.io/ztlctl/session-recall" docs/llms-full.txt returns >= 1 match (URL blockquote present)
  </acceptance_criteria>
  <done>llms-full.txt contains full content sections for all 5 v3.0 feature pages with URL blockquote headers, and existing sections have accurate v3.0 tool/resource counts.</done>
</task>

</tasks>

<verification>
- `grep -c "session-recall\|polaris\|contradiction-detection\|media-ingestion\|methodology" docs/llms.txt` returns 5+ matches
- `grep "thatdevstudio.github.io/ztlctl/session-recall" docs/llms-full.txt` returns a match
- `grep "thatdevstudio.github.io/ztlctl/polaris" docs/llms-full.txt` returns a match
- No stale "59 MCP tools" or "59 tools" remains in either file (replaced with 73+)
</verification>

<success_criteria>
- llms.txt lists all 5 new v3.0 feature pages with descriptions
- llms-full.txt contains full content sections for all 5 new pages with URL blockquote headers
- Existing descriptions in both files use accurate v3.0 numbers (73+ tools, 20 resources)
- An agent reading llms.txt can discover session recall, polaris, contradiction detection, media ingestion, and methodology
</success_criteria>

<output>
After completion, create `.planning/phases/26-existing-pages-and-quality-pass/26-02-SUMMARY.md`
</output>
