---
phase: 32-validation-and-hardening
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - .planning/phases/32-validation-and-hardening/32-02-SUMMARY.md
autonomous: false
requirements:
  - PLGN-01
  - SKIL-01
  - SKIL-02
  - SKIL-03
  - SKIL-04
  - SKIL-05
  - SKIL-06
  - SKIL-07
  - SKIL-08
  - SKIL-09
  - SKIL-10
  - DIST-01
  - DIST-03

must_haves:
  truths:
    - "Plugin validates cleanly under claude plugin validate"
    - "All skills load without exceeding 2% context budget"
    - "Plugin installs correctly from marketplace and all components are available"
    - "Human has verified installed-state behavior"
  artifacts:
    - path: ".planning/phases/32-validation-and-hardening/32-02-SUMMARY.md"
      provides: "Human verification results and final sign-off"
  key_links:
    - from: "plugin/"
      to: "claude plugin validate"
      via: "CLI validation command"
      pattern: "zero warnings"
---

<objective>
Human verification checklist for items that cannot be automated: installed-state testing, context budget measurement, and `claude plugin validate` execution.

Purpose: The distribution checklist includes items that require a running Claude Code instance or installed plugin state. These cannot be tested in CI — they require human verification.
Output: Verified checklist with pass/fail results documented in SUMMARY
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-validation-and-hardening/32-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verification — installed state and context budget</name>
  <files>.planning/phases/32-validation-and-hardening/32-02-SUMMARY.md</files>
  <action>
Present the human verification checklist to the user. All automated validation tests from Plan 01 are passing. The following items from the PITFALLS.md distribution checklist require human verification because they need a running Claude Code instance with the plugin installed. After user provides results, document them in the SUMMARY.
  </action>
  <verify>User has provided pass/fail results for all 6 verification items</verify>
  <done>All 6 verification items pass. Any failures are documented with specific details for follow-up.</done>
  <read_first>
    - .planning/phases/32-validation-and-hardening/32-01-SUMMARY.md
  </read_first>
  <acceptance_criteria>All 6 verification items pass. Any failures are documented with specific details for follow-up.</acceptance_criteria>
  <how-to-verify>
Run the following checks and record pass/fail for each:

**1. `claude plugin validate` (PITFALLS checklist item 1)**
```bash
cd plugin/
claude plugin validate
```
Expected: Zero warnings, zero errors.

**2. Context budget check (PITFALLS checklist item 14, Roadmap success criteria 2)**
- Start a Claude Code session with the plugin installed
- Type `/context` or check context usage
- Verify all 13 skills consume less than 2% of the context budget
- Record the actual percentage

**3. Installed-state skill availability (PITFALLS checklist item 10, Roadmap success criteria 1)**
- Install the plugin: `claude plugin install --plugin-dir ./plugin` (or from marketplace if published)
- Verify `/ztlctl:session`, `/ztlctl:capture`, `/ztlctl:review`, `/ztlctl:seed`, `/ztlctl:align` all appear in slash command autocomplete
- Verify `claude mcp list` shows the ztlctl server

**4. Installed-state skill activation (Roadmap success criteria 1)**
In the Claude Code session with plugin installed and a vault initialized:
- Test orient: ask "What's in my vault?" -- should activate `ztl:orient`
- Test session: type `/ztlctl:session "test topic"` -- should activate `ztl:session`
- Test capture: type `/ztlctl:capture "test note"` -- should activate `ztl:capture`

**5. Agent availability**
- Verify both research and maintenance agents are available
- Test: ask "Research what I know about [topic]" -- should offer the research agent

**6. macOS + Linux documentation status (PITFALLS checklist item 11)**
- Confirm README covers macOS installation (primary platform)
- Confirm README notes Linux/Windows status

Record results as: PASS / FAIL (with notes) for each item.
  </how-to-verify>
  <resume-signal>Type results as "PASS" or "FAIL: [reason]" for each of the 6 items, or type "all pass" if everything checks out.</resume-signal>
</task>

</tasks>

<verification>
Human verification results documented in SUMMARY with pass/fail for each checklist item.
</verification>

<success_criteria>
- `claude plugin validate` reports zero warnings
- Context budget is under 2% for all 13 skills combined
- Plugin installs correctly and all components are available under installed state
- At least 3 skills activate correctly on natural language prompts
- Both agents are available and correctly described
- Results documented in 32-02-SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/32-validation-and-hardening/32-02-SUMMARY.md`
</output>
