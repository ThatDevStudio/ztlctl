---
phase: 28-plugin-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/.claude-plugin/plugin.json
  - plugin/.mcp.json
  - plugin/hooks/hooks.json
  - plugin/hooks/scripts/vault-gate.sh
  - tests/plugin/test_plugin_structure.py
  - tests/plugin/__init__.py
autonomous: true
requirements: [PLGN-01, PLGN-02, PLGN-03]

must_haves:
  truths:
    - "plugin.json manifest contains all required fields and passes structural validation"
    - "MCP stdio transport produces zero non-JSON bytes on stdout"
    - "Any mcp__ztlctl__* tool call without an initialized vault is blocked with a user-friendly error"
  artifacts:
    - path: "plugin/.claude-plugin/plugin.json"
      provides: "Plugin manifest with all required fields"
      contains: "\"name\": \"ztlctl\""
    - path: "plugin/.mcp.json"
      provides: "MCP server config with PYTHONUNBUFFERED=1"
      contains: "PYTHONUNBUFFERED"
    - path: "plugin/hooks/scripts/vault-gate.sh"
      provides: "PreToolUse hook that blocks tool calls when no vault exists"
      contains: "exit 2"
    - path: "plugin/hooks/hooks.json"
      provides: "Hook registry with both SessionStart and PreToolUse hooks"
      contains: "PreToolUse"
    - path: "tests/plugin/test_plugin_structure.py"
      provides: "Automated validation of plugin layout, manifest, and stdout cleanliness"
      contains: "test_plugin_directory_structure"
  key_links:
    - from: "plugin/hooks/hooks.json"
      to: "plugin/hooks/scripts/vault-gate.sh"
      via: "command field referencing ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/vault-gate.sh"
      pattern: "vault-gate\\.sh"
    - from: "plugin/.mcp.json"
      to: "ztlctl serve"
      via: "MCP stdio server launch command"
      pattern: "ztlctl.*serve"
---

<objective>
Harden the existing plugin scaffold so the manifest, MCP transport, and vault gate hook meet Claude Code plugin conventions.

Purpose: PLGN-01 (manifest), PLGN-02 (clean stdio), and PLGN-03 (vault gate) are the foundation that all subsequent skill/command/agent phases depend on. Without clean transport and a vault gate, skills will fail silently or corrupt state.

Output: Production-ready plugin manifest, clean MCP transport config, vault gate PreToolUse hook, and automated test suite validating all three.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden plugin manifest, MCP transport config, and vault gate hook</name>
  <files>
    plugin/.claude-plugin/plugin.json,
    plugin/.mcp.json,
    plugin/hooks/hooks.json,
    plugin/hooks/scripts/vault-gate.sh
  </files>
  <read_first>
    plugin/.claude-plugin/plugin.json,
    plugin/.mcp.json,
    plugin/hooks/hooks.json,
    plugin/hooks/scripts/session-context.sh,
    .planning/research/PITFALLS.md
  </read_first>
  <action>
**1. Update plugin/.claude-plugin/plugin.json** (per PLGN-01, PITFALLS #1, #17):

The existing manifest is mostly correct. Keep `"name": "ztlctl"` (already kebab-case). Ensure ALL required fields are present. The final plugin.json must contain exactly:

```json
{
  "name": "ztlctl",
  "version": "1.0.0",
  "description": "Agentic research assistant for ztlctl knowledge vaults",
  "author": {
    "name": "ThatDev Studio",
    "url": "https://github.com/ThatDevStudio/ztlctl"
  },
  "repository": "https://github.com/ThatDevStudio/ztlctl",
  "license": "MIT",
  "keywords": ["zettelkasten", "knowledge-management", "research", "notes", "agentic"],
  "commands": "./commands",
  "agents": ["./agents"],
  "hooks": "./hooks/hooks.json"
}
```

This is essentially the same as the current file. No changes needed if it already matches.

**2. Update plugin/.mcp.json** (per PLGN-02, PITFALLS #2):

Add `PYTHONUNBUFFERED=1` to the env block to prevent buffering stalls on stdio transport. The final .mcp.json must be:

```json
{
  "mcpServers": {
    "ztlctl": {
      "command": "ztlctl",
      "args": ["serve"],
      "env": {
        "PYTHONUNBUFFERED": "1"
      }
    }
  }
}
```

**3. Update plugin/hooks/hooks.json** (per PLGN-03, PITFALLS #9, #11):

Add a PreToolUse hook entry for the vault gate. The hook must:
- Match only `mcp__ztlctl__*` tool calls (using `matcher` field to scope to ztlctl tools)
- Call `vault-gate.sh` script via `${CLAUDE_PLUGIN_ROOT}`
- Have a reasonable timeout (5 seconds)

The final hooks.json must be:

```json
{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/session-context.sh",
            "timeout": 10
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "mcp__ztlctl__",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/vault-gate.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
```

**4. Create plugin/hooks/scripts/vault-gate.sh** (per PLGN-03, PITFALLS #9):

This PreToolUse hook checks if a ztlctl vault is initialized. If not, it exits with code 2 (blocking) and writes a user-friendly error to stderr. If vault exists, it exits 0 (pass).

The script must:
- Use `#!/usr/bin/env bash` shebang
- Use `set -euo pipefail`
- Check if `ztlctl` command exists; if not, exit 2 with message "ztlctl is not installed. Install it with: pip install ztlctl (or brew install ztlctl)"
- Run `ztlctl --json check vault-exists 2>/dev/null` — but since this command may not exist, use a simpler approach: check if `ztlctl.toml` or `.ztlctl.toml` exists in the current directory or any parent directory (up to $HOME). The simplest reliable check: run `ztlctl --json query list --limit 0 2>/dev/null` and check exit code — if non-zero, vault is not initialized.
- Actually, the most reliable approach: look for `ztlctl.toml` in CWD or parents. Walk up from $PWD checking for `ztlctl.toml` or `.ztlctl/config.toml`. If none found, vault is not initialized.
- On vault not found: `echo "No ztlctl vault found in this directory. Run 'ztlctl init' to create one, or navigate to an existing vault directory." >&2` then `exit 2`
- On vault found: `exit 0`
- Must have execute permission: after creating, run `chmod +x plugin/hooks/scripts/vault-gate.sh`

Implementation:

```bash
#!/usr/bin/env bash
# PreToolUse hook: block mcp__ztlctl__* calls when no vault is initialized.
# Exit 0 = allow tool call, Exit 2 = block tool call (stderr shown to Claude).

set -euo pipefail

# Check ztlctl is installed
if ! command -v ztlctl >/dev/null 2>&1; then
  echo "ztlctl is not installed. Install it with: pip install ztlctl (or brew install ztlctl)" >&2
  exit 2
fi

# Walk up from CWD looking for vault config
check_dir="$PWD"
while true; do
  if [ -f "${check_dir}/ztlctl.toml" ]; then
    exit 0
  fi
  parent="$(dirname "${check_dir}")"
  if [ "${parent}" = "${check_dir}" ]; then
    break
  fi
  check_dir="${parent}"
done

echo "No ztlctl vault found in this directory. Run 'ztlctl init' to create one, or navigate to an existing vault directory." >&2
exit 2
```

After creating vault-gate.sh, set execute permission:
```bash
chmod +x plugin/hooks/scripts/vault-gate.sh
```

Also verify session-context.sh has execute permission:
```bash
chmod +x plugin/hooks/scripts/session-context.sh
```
  </action>
  <verify>
    <automated>bash -c "test -x plugin/hooks/scripts/vault-gate.sh && test -x plugin/hooks/scripts/session-context.sh && python -c \"import json; d=json.load(open('plugin/.mcp.json')); assert d['mcpServers']['ztlctl']['env']['PYTHONUNBUFFERED']=='1'\" && python -c \"import json; d=json.load(open('plugin/hooks/hooks.json')); assert 'PreToolUse' in d['hooks']\" && echo PASS"</automated>
  </verify>
  <acceptance_criteria>
    - plugin/.mcp.json contains `"PYTHONUNBUFFERED": "1"` in the env block
    - plugin/hooks/hooks.json contains a `"PreToolUse"` key with a matcher containing `"mcp__ztlctl__"`
    - plugin/hooks/hooks.json PreToolUse hook command contains `vault-gate.sh`
    - plugin/hooks/scripts/vault-gate.sh exists and has execute permission (`-x`)
    - plugin/hooks/scripts/vault-gate.sh contains `exit 2` (blocking exit code)
    - plugin/hooks/scripts/vault-gate.sh contains `ztlctl.toml` (vault config check)
    - plugin/hooks/scripts/vault-gate.sh contains `>&2` (stderr output for user-friendly message)
    - plugin/hooks/scripts/session-context.sh has execute permission (`-x`)
    - plugin/.claude-plugin/plugin.json contains `"name": "ztlctl"`
    - plugin/.claude-plugin/plugin.json contains `"hooks": "./hooks/hooks.json"`
  </acceptance_criteria>
  <done>Plugin manifest has all required fields, .mcp.json has PYTHONUNBUFFERED=1, hooks.json registers both SessionStart and PreToolUse hooks, vault-gate.sh blocks tool calls when no vault config file is found (exit 2 with guidance message to stderr), all hook scripts have execute permissions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create automated plugin structure and stdout cleanliness tests</name>
  <files>
    tests/plugin/__init__.py,
    tests/plugin/test_plugin_structure.py
  </files>
  <read_first>
    plugin/.claude-plugin/plugin.json,
    plugin/.mcp.json,
    plugin/hooks/hooks.json,
    plugin/hooks/scripts/vault-gate.sh,
    tests/mcp/test_stdio_integration.py
  </read_first>
  <behavior>
    - test_plugin_directory_structure: plugin/ has .claude-plugin/, skills/, agents/, hooks/, commands/ at root level; .claude-plugin/ contains ONLY plugin.json (no skills/hooks/commands inside it)
    - test_plugin_json_required_fields: plugin.json has name, version, description, author, repository, license, commands, hooks fields
    - test_plugin_json_name_kebab_case: plugin.json name matches ^[a-z][a-z0-9-]*$ regex
    - test_mcp_json_has_pythonunbuffered: .mcp.json mcpServers.ztlctl.env.PYTHONUNBUFFERED == "1"
    - test_hook_scripts_executable: all .sh files under plugin/hooks/scripts/ have execute permission
    - test_vault_gate_hook_registered: hooks.json has PreToolUse entry with matcher containing "mcp__ztlctl__" and command containing "vault-gate.sh"
    - test_vault_gate_blocks_without_vault: running vault-gate.sh in a directory with no ztlctl.toml exits with code 2 and stderr contains "ztlctl init"
    - test_vault_gate_passes_with_vault: running vault-gate.sh in a directory containing ztlctl.toml exits with code 0
    - test_no_paths_outside_plugin_root: no file in plugin/ contains "../" path traversals (per PITFALLS #12)
    - test_hook_paths_use_plugin_root_var: all command fields in hooks.json use ${CLAUDE_PLUGIN_ROOT} (per PITFALLS #11)
    - test_stdio_no_stdout_pollution: launching `python -m ztlctl serve --transport stdio` and sending an initialize JSON-RPC request produces only valid JSON on stdout (no banners, no print output)
  </behavior>
  <action>
Create `tests/plugin/__init__.py` as an empty file.

Create `tests/plugin/test_plugin_structure.py` with the following tests. All tests use the project root to locate `plugin/` directory. The tests validate:

1. **Directory structure** (PLGN-01): Assert `plugin/.claude-plugin/plugin.json` exists. Assert `plugin/skills/`, `plugin/agents/`, `plugin/hooks/`, `plugin/commands/` exist at root level. Assert no `skills/`, `agents/`, `hooks/`, or `commands/` directories exist inside `plugin/.claude-plugin/`. Assert `plugin/.mcp.json` exists at root level.

2. **Manifest fields** (PLGN-01): Load `plugin/.claude-plugin/plugin.json`, assert keys `name`, `version`, `description`, `author`, `repository`, `license`, `commands`, `hooks` all present.

3. **Name kebab-case** (PLGN-01): Assert `plugin.json` `name` matches `re.fullmatch(r"[a-z][a-z0-9-]*", name)`.

4. **PYTHONUNBUFFERED** (PLGN-02): Load `plugin/.mcp.json`, assert `data["mcpServers"]["ztlctl"]["env"]["PYTHONUNBUFFERED"] == "1"`.

5. **Hook scripts executable** (PLGN-01): Glob `plugin/hooks/scripts/*.sh`, assert each has `os.access(path, os.X_OK)`.

6. **Vault gate registered** (PLGN-03): Load `plugin/hooks/hooks.json`, assert `"PreToolUse"` key exists, assert any entry has `matcher` containing `"mcp__ztlctl__"` and `command` containing `"vault-gate.sh"`.

7. **Vault gate blocks without vault** (PLGN-03): Use `subprocess.run` to execute `bash plugin/hooks/scripts/vault-gate.sh` with `cwd=tmp_path` (empty temp dir). Assert returncode == 2. Assert `"ztlctl init"` in stderr output.

8. **Vault gate passes with vault** (PLGN-03): Create `ztlctl.toml` in `tmp_path`, run vault-gate.sh with `cwd=tmp_path`. Assert returncode == 0.

9. **No path traversals** (PLGN-01): Walk all files in `plugin/`, read each text file, assert `"../"` not in content (skip binary files with try/except UnicodeDecodeError).

10. **Hook paths use CLAUDE_PLUGIN_ROOT** (PLGN-01): Load hooks.json, for each hook entry with a `command` field, assert `"${CLAUDE_PLUGIN_ROOT}"` is in the command string.

11. **Stdout cleanliness** (PLGN-02): Use `subprocess.run` to launch `sys.executable -m ztlctl --sync -c {config_path} serve --transport stdio` with a JSON-RPC initialize request piped to stdin. Capture stdout. Assert every non-empty line of stdout is valid JSON (use `json.loads`). This test needs a temp ztlctl.toml config file (same pattern as `tests/mcp/test_stdio_integration.py`). Mark with `@pytest.mark.slow` if desired.

Use `pathlib.Path(__file__).resolve().parent.parent.parent` to find project root (tests/plugin/test_plugin_structure.py -> project root). Store as `PLUGIN_DIR = PROJECT_ROOT / "plugin"`.

Import: `json`, `os`, `re`, `subprocess`, `sys`, `pathlib.Path`, `pytest`, `textwrap`.
  </action>
  <verify>
    <automated>cd /Users/shparki/Documents/Workspace/thatdev/ztlctl && uv run pytest tests/plugin/test_plugin_structure.py -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/plugin/__init__.py exists (empty file)
    - tests/plugin/test_plugin_structure.py exists
    - test_plugin_structure.py contains `def test_plugin_directory_structure`
    - test_plugin_structure.py contains `def test_plugin_json_required_fields`
    - test_plugin_structure.py contains `def test_plugin_json_name_kebab_case`
    - test_plugin_structure.py contains `def test_mcp_json_has_pythonunbuffered`
    - test_plugin_structure.py contains `def test_hook_scripts_executable`
    - test_plugin_structure.py contains `def test_vault_gate_hook_registered`
    - test_plugin_structure.py contains `def test_vault_gate_blocks_without_vault`
    - test_plugin_structure.py contains `def test_vault_gate_passes_with_vault`
    - test_plugin_structure.py contains `def test_no_paths_outside_plugin_root`
    - test_plugin_structure.py contains `def test_hook_paths_use_plugin_root_var`
    - test_plugin_structure.py contains `def test_stdio_no_stdout_pollution`
    - `uv run pytest tests/plugin/test_plugin_structure.py` exits 0 (all tests pass)
  </acceptance_criteria>
  <done>13 tests validate plugin directory structure, manifest fields, kebab-case naming, PYTHONUNBUFFERED env var, hook script permissions, vault gate registration, vault gate blocking behavior (exit 2 without vault, exit 0 with vault), no path traversals, CLAUDE_PLUGIN_ROOT usage, and MCP stdio stdout cleanliness. All tests pass.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/plugin/ -v` — all 13 tests pass
- `python -c "import json; json.load(open('plugin/.claude-plugin/plugin.json'))"` — valid JSON
- `python -c "import json; json.load(open('plugin/.mcp.json'))"` — valid JSON
- `python -c "import json; json.load(open('plugin/hooks/hooks.json'))"` — valid JSON
- `test -x plugin/hooks/scripts/vault-gate.sh` — executable
- `test -x plugin/hooks/scripts/session-context.sh` — executable
</verification>

<success_criteria>
- Plugin manifest has all required fields and passes structural validation tests
- MCP transport config includes PYTHONUNBUFFERED=1 and stdio produces only valid JSON
- Vault gate hook blocks with exit 2 when no vault, passes with exit 0 when vault exists
- All 13 automated tests pass in the test suite
</success_criteria>

<output>
After completion, create `.planning/phases/28-plugin-foundation/28-01-SUMMARY.md`
</output>
