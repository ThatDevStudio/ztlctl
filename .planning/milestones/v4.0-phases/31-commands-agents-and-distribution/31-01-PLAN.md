---
phase: 31-commands-agents-and-distribution
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/commands/session.md
  - plugin/commands/capture.md
  - plugin/commands/review.md
  - plugin/commands/seed.md
  - plugin/commands/align.md
  - plugin/agents/vault-analyst.md
  - plugin/agents/knowledge-synthesizer.md
  - plugin/agents/research.md
  - plugin/agents/maintenance.md
autonomous: true
requirements: [CMDA-01, CMDA-02, CMDA-03]

must_haves:
  truths:
    - "Typing /ztlctl:session <topic> delegates to the ztl:session skill with argument passthrough"
    - "Typing /ztlctl:capture delegates to the ztl:capture skill"
    - "Typing /ztlctl:review delegates to the ztl:review-triage skill"
    - "Typing /ztlctl:seed <title> delegates to the ztl:capture skill for seed creation"
    - "Typing /ztlctl:align <decision> delegates to the ztl:align skill"
    - "Research agent has a read-only tool allowlist constrained to search, get, graph, and topic_packet tools"
    - "Maintenance agent has full mcp__ztlctl__* tools with confirmation-before-mutation system prompt"
    - "Agent frontmatter contains only supported fields (name, description, model, maxTurns, tools) -- no hooks, mcpServers, or permissionMode"
  artifacts:
    - path: "plugin/commands/session.md"
      provides: "Slash command delegating to ztl:session skill"
      contains: "ztl:session"
    - path: "plugin/commands/capture.md"
      provides: "Slash command delegating to ztl:capture skill"
      contains: "ztl:capture"
    - path: "plugin/commands/review.md"
      provides: "Slash command delegating to ztl:review-triage skill"
      contains: "ztl:review-triage"
    - path: "plugin/commands/seed.md"
      provides: "Slash command delegating to ztl:capture skill for seeds"
      contains: "ztl:capture"
    - path: "plugin/commands/align.md"
      provides: "Slash command delegating to ztl:align skill"
      contains: "ztl:align"
    - path: "plugin/agents/research.md"
      provides: "Read-only autonomous research agent"
      contains: "mcp__ztlctl__search"
    - path: "plugin/agents/maintenance.md"
      provides: "Autonomous maintenance agent with confirmation gates"
      contains: "mcp__ztlctl__check_integrity"
  key_links:
    - from: "plugin/commands/session.md"
      to: "plugin/skills/session/SKILL.md"
      via: "skill reference in command body"
      pattern: "ztl:session"
    - from: "plugin/agents/research.md"
      to: "mcp__ztlctl__* tools"
      via: "tools frontmatter allowlist"
      pattern: "mcp__ztlctl__search"
---

<objective>
Replace all existing scaffold commands and agents with properly wired versions that delegate to the 10 skills built in Phases 29-30, and create two new autonomous agents (research, maintenance) with constrained tool allowlists.

Purpose: Commands are the user-facing entry points to skills; agents enable autonomous multi-step workflows. The existing scaffolds reference wrong MCP resources and do not invoke skills.
Output: 5 command files, 2 new agent files (old agents deleted)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-commands-agents-and-distribution/31-CONTEXT.md
@.planning/research/PITFALLS.md

@plugin/commands/session.md
@plugin/commands/capture.md
@plugin/commands/review.md
@plugin/commands/seed.md
@plugin/agents/vault-analyst.md
@plugin/agents/knowledge-synthesizer.md
@plugin/.claude-plugin/plugin.json
</context>

<interfaces>
<!-- Existing skill names that commands must reference -->
Skills in plugin/skills/:
- orient/SKILL.md -> ztl:orient
- session/SKILL.md -> ztl:session
- capture/SKILL.md -> ztl:capture
- review-triage/SKILL.md -> ztl:review-triage
- align/SKILL.md -> ztl:align
- synthesize/SKILL.md -> ztl:synthesize
- decision-support/SKILL.md -> ztl:decision-support
- orient-session/SKILL.md -> ztl:orient-session
- garden-health/SKILL.md -> ztl:garden-health
- review-contradictions/SKILL.md -> ztl:review-contradictions

<!-- Command file format (YAML frontmatter + markdown body) -->
Command frontmatter fields: description, argument-hint
Command body: instructions that delegate to the skill via $ARGUMENTS passthrough

<!-- Agent frontmatter fields (per PITFALLS #19 -- plugin agents ONLY support these) -->
Supported: name, description, model, effort, maxTurns, tools, disallowedTools, skills, memory, background, isolation
NOT supported (will silently fail): hooks, mcpServers, permissionMode
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace 4 existing commands and add align command</name>
  <files>plugin/commands/session.md, plugin/commands/capture.md, plugin/commands/review.md, plugin/commands/seed.md, plugin/commands/align.md</files>
  <read_first>plugin/commands/session.md, plugin/commands/capture.md, plugin/commands/review.md, plugin/commands/seed.md, plugin/skills/session/SKILL.md, plugin/skills/capture/SKILL.md, plugin/skills/review-triage/SKILL.md, plugin/skills/align/SKILL.md</read_first>
  <action>
Replace all 4 existing command files entirely (they reference wrong MCP resources like `ztlctl://context` and `create_log`). Create 1 new command file. Each command is a thin delegation to the corresponding skill with $ARGUMENTS passthrough. Per CONTEXT.md decisions, commands use minimal body.

**session.md** — REPLACE entirely:
- Frontmatter: `description: Start a structured research session in the vault`, `argument-hint: <topic>`
- Body: Invoke the `ztl:session` skill with `$ARGUMENTS` as the topic. If no arguments, the skill will prompt for a topic. Do not duplicate the session lifecycle logic from the skill — delegate entirely.

**capture.md** — REPLACE entirely:
- Frontmatter: `description: Guided knowledge capture with duplicate checking`, `argument-hint: [title or topic]`
- Body: Invoke the `ztl:capture` skill with `$ARGUMENTS` as context for what to capture. The skill handles type detection, duplicate checking, and reweave.

**review.md** — REPLACE entirely:
- Frontmatter: `description: Review vault health and triage actionable items`
- Body: Invoke the `ztl:review-triage` skill. No arguments needed — the skill runs a full vault health check and surfaces actionable items with confirmation gates.

**seed.md** — REPLACE entirely:
- Frontmatter: `description: Quick-capture a seed note with minimal ceremony`, `argument-hint: <title> [tags...]`
- Body: Invoke the `ztl:capture` skill with instructions to create a seed specifically (minimal ceremony, no duplicate checking). Pass `$ARGUMENTS` as the seed title and optional tags.

**align.md** — CREATE new:
- Frontmatter: `description: Check if a decision aligns with vault priorities`, `argument-hint: <decision or question>`
- Body: Invoke the `ztl:align` skill with `$ARGUMENTS` as the decision to evaluate. The skill reads polaris, runs alignment check, and presents analysis.

Each command body should be 5-15 lines max. The pattern is: brief context sentence, then "Use the ztl:X skill to..." with $ARGUMENTS passthrough. Do NOT duplicate skill workflow logic in commands.
  </action>
  <verify>
    <automated>cd /Users/shparki/Documents/Workspace/thatdev/ztlctl && for f in plugin/commands/{session,capture,review,seed,align}.md; do echo "=== $f ===" && head -3 "$f" && echo "---" && grep -c "ztl:" "$f"; done</automated>
  </verify>
  <acceptance_criteria>
- All 5 command files exist in plugin/commands/
- Each file has YAML frontmatter with description field
- session.md references ztl:session skill
- capture.md references ztl:capture skill
- review.md references ztl:review-triage skill
- seed.md references ztl:capture skill (seed mode)
- align.md references ztl:align skill
- Each file contains $ARGUMENTS passthrough where applicable
- No command body exceeds 15 lines of instructions
- No command references old MCP resources (ztlctl://context, create_log, ztlctl://overview)
  </acceptance_criteria>
  <done>5 command files exist, each delegating to the correct skill with argument passthrough, no legacy MCP resource references</done>
</task>

<task type="auto">
  <name>Task 2: Replace 2 scaffold agents with research and maintenance agents</name>
  <files>plugin/agents/research.md, plugin/agents/maintenance.md, plugin/agents/vault-analyst.md, plugin/agents/knowledge-synthesizer.md</files>
  <read_first>plugin/agents/vault-analyst.md, plugin/agents/knowledge-synthesizer.md, .planning/phases/31-commands-agents-and-distribution/31-CONTEXT.md, .planning/research/PITFALLS.md</read_first>
  <action>
Delete the 2 existing scaffold agents (vault-analyst.md, knowledge-synthesizer.md) and create 2 new agents. Per PITFALLS #19, agent frontmatter must ONLY use supported fields: name, description, model, maxTurns, tools. NO hooks, mcpServers, or permissionMode fields.

**DELETE** plugin/agents/vault-analyst.md
**DELETE** plugin/agents/knowledge-synthesizer.md

**research.md** — CREATE new:
- Frontmatter:
  - `name: research`
  - `description`: Use when user asks to deeply explore a topic, find connections across notes, or assemble research findings from the vault. Examples block with 3 examples: "research X in my vault", "what do I know about Y", "find connections between A and B"
  - `model: sonnet` (read-heavy, cost-efficient)
  - `maxTurns: 15` (depth limit per CONTEXT.md decision)
  - `tools`: READ-ONLY allowlist: `["mcp__ztlctl__search", "mcp__ztlctl__get_document", "mcp__ztlctl__get_related", "mcp__ztlctl__graph_themes", "mcp__ztlctl__graph_rank", "mcp__ztlctl__graph_gaps", "mcp__ztlctl__graph_path", "mcp__ztlctl__graph_bridges", "mcp__ztlctl__topic_packet"]`
- System prompt body: You are a vault research agent. Your job is to autonomously explore the vault on a given topic, following graph connections and assembling findings. Process: (1) Search broadly, (2) Follow graph connections via get_related, (3) Identify themes and gaps, (4) Assemble a structured research brief. Stop when you have explored all relevant connections or hit your turn limit. Present findings as a structured report with source references (IDs and titles). You are READ-ONLY — you cannot create, modify, or delete any vault content.

**maintenance.md** — CREATE new:
- Frontmatter:
  - `name: maintenance`
  - `description`: Use when the user asks to run vault maintenance, clean up the vault, check health, scan for contradictions, or review garden status. Examples block with 3 examples: "run maintenance on my vault", "clean up my vault", "check vault health and fix issues"
  - `model: sonnet` (cost-efficient for batch operations)
  - `maxTurns: 20` (per CONTEXT.md decision)
  - `tools`: Full ztlctl allowlist: `["mcp__ztlctl__*"]` (needs write access for repairs)
- System prompt body: You are a vault maintenance agent. Your job is to run comprehensive vault health operations. Process: (1) Run check_integrity to scan for structural issues, (2) Run check_contradictions to surface contradiction candidates (present for human evaluation, NEVER auto-confirm per iron law), (3) Review garden status via vault_review and work_queue, (4) Present a maintenance report with all findings. CRITICAL RULE: Before ANY mutation (reweave, confirm_contradiction, status update), you MUST present what you intend to do and wait for explicit user confirmation. Never auto-execute writes. Present findings as a structured maintenance report organized by category (integrity, contradictions, garden, work queue).
  </action>
  <verify>
    <automated>cd /Users/shparki/Documents/Workspace/thatdev/ztlctl && echo "=== Files ===" && ls plugin/agents/ && echo "=== No unsupported fields ===" && for f in plugin/agents/*.md; do echo "$f:"; grep -c "hooks:\|mcpServers:\|permissionMode:" "$f" || echo "0 (clean)"; done && echo "=== Required fields ===" && for f in plugin/agents/*.md; do echo "$f:"; grep -c "^name:\|^  name:" "$f"; grep -c "maxTurns:" "$f"; grep -c "tools:" "$f"; done</automated>
  </verify>
  <acceptance_criteria>
- plugin/agents/ contains exactly 2 files: research.md and maintenance.md
- vault-analyst.md and knowledge-synthesizer.md are deleted
- research.md has name, description (with examples), model: sonnet, maxTurns: 15, tools (read-only list)
- maintenance.md has name, description (with examples), model: sonnet, maxTurns: 20, tools
- Neither agent uses hooks, mcpServers, or permissionMode fields
- research.md tools list contains ONLY read-only tools (no create_*, no reweave, no session_*, no confirm_*)
- maintenance.md system prompt includes confirmation-before-mutation instruction
- maintenance.md system prompt includes never-auto-confirm rule for contradictions
  </acceptance_criteria>
  <done>2 new agents exist with correct frontmatter (no unsupported fields), research agent is read-only constrained, maintenance agent has confirmation gates, old scaffold agents are deleted</done>
</task>

</tasks>

<verification>
- `ls plugin/commands/` shows exactly 5 files: session.md, capture.md, review.md, seed.md, align.md
- `ls plugin/agents/` shows exactly 2 files: research.md, maintenance.md
- `grep -r "ztlctl://context\|ztlctl://overview\|create_log" plugin/commands/` returns no results (no legacy references)
- `grep -r "hooks:\|mcpServers:\|permissionMode:" plugin/agents/` returns no results (no unsupported fields)
- `grep -l "ztl:session" plugin/commands/session.md` finds the skill reference
- `grep -l "ztl:align" plugin/commands/align.md` finds the skill reference
- `grep "maxTurns: 15" plugin/agents/research.md` confirms depth limit
- `grep "maxTurns: 20" plugin/agents/maintenance.md` confirms turn budget
</verification>

<success_criteria>
- 5 slash commands exist, each delegating to the correct skill with argument passthrough
- 2 autonomous agents exist with constrained tool allowlists and correct frontmatter
- No legacy scaffold commands or agents remain
- No unsupported agent frontmatter fields (hooks, mcpServers, permissionMode)
- Research agent is strictly read-only (no write tools in allowlist)
- Maintenance agent includes confirmation-before-mutation instructions
</success_criteria>

<output>
After completion, create `.planning/phases/31-commands-agents-and-distribution/31-01-SUMMARY.md`
</output>
