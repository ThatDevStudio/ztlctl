---
phase: 31-commands-agents-and-distribution
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - marketplace.json
  - .github/workflows/release-pipeline.yml
  - plugin/README.md
  - plugin/.claude-plugin/plugin.json
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03]

must_haves:
  truths:
    - "marketplace.json at repo root points to plugin/ subdirectory for git-subdir installation"
    - "Running claude plugin install from the GitHub repo installs the plugin from the plugin/ subdirectory"
    - "After a release merges to develop, plugin.json version is bumped alongside pyproject.toml automatically"
    - "Plugin README documents prerequisites (Python 3.13, uv/pipx), install command, post-install verification, and troubleshooting"
    - "plugin.json version field is updated to reflect current commands and agents count"
  artifacts:
    - path: "marketplace.json"
      provides: "Git-subdir marketplace source pointing to plugin/"
      contains: "plugin"
    - path: ".github/workflows/release-pipeline.yml"
      provides: "Version sync step copying pyproject.toml version to plugin.json"
      contains: "plugin.json"
    - path: "plugin/README.md"
      provides: "Installation documentation with prerequisites and troubleshooting"
      contains: "claude plugin install"
    - path: "plugin/.claude-plugin/plugin.json"
      provides: "Updated manifest with correct component counts"
      contains: "version"
  key_links:
    - from: "marketplace.json"
      to: "plugin/.claude-plugin/plugin.json"
      via: "git-subdir directory reference"
      pattern: "plugin"
    - from: ".github/workflows/release-pipeline.yml"
      to: "plugin/.claude-plugin/plugin.json"
      via: "version sync step after cz bump"
      pattern: "plugin.json"
---

<objective>
Create the marketplace distribution configuration, add version synchronization to the release pipeline, and rewrite the plugin README with comprehensive installation documentation.

Purpose: Users must be able to install the plugin via `claude plugin install ztlctl` and receive version updates automatically. The README is the primary onboarding surface.
Output: marketplace.json (new), updated release-pipeline.yml, rewritten plugin/README.md, updated plugin.json
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-commands-agents-and-distribution/31-CONTEXT.md
@.planning/research/PITFALLS.md

@.github/workflows/release-pipeline.yml
@plugin/.claude-plugin/plugin.json
@plugin/README.md
</context>

<interfaces>
<!-- Current plugin.json structure -->
```json
{
  "name": "ztlctl",
  "version": "1.0.0",
  "description": "Agentic research assistant for ztlctl knowledge vaults",
  "author": { "name": "ThatDev Studio", "url": "https://github.com/ThatDevStudio/ztlctl" },
  "repository": "https://github.com/ThatDevStudio/ztlctl",
  "license": "MIT",
  "commands": "./commands",
  "agents": ["./agents"],
  "hooks": "./hooks/hooks.json"
}
```

<!-- Release pipeline structure -->
The release job has these key steps in order:
1. Configure release mode
2. Set up uv / sync deps
3. Configure git identity
4. Bump version and changelog (cz bump)
5. Release candidate validation gates
6. Build release manifest
7. Push release commit and tag
8. Ensure GitHub release exists
9. Upload release assets

Version sync step must go AFTER step 4 (cz bump) and BEFORE step 7 (push).

<!-- pyproject.toml version location -->
Version is managed by commitizen in pyproject.toml, readable via: `uv run cz version --project`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create marketplace.json and update plugin.json manifest</name>
  <files>marketplace.json, plugin/.claude-plugin/plugin.json</files>
  <read_first>plugin/.claude-plugin/plugin.json, .planning/phases/31-commands-agents-and-distribution/31-CONTEXT.md</read_first>
  <action>
**marketplace.json** — CREATE at repo root (not inside plugin/):
Create the marketplace manifest that tells Claude Code where to find the plugin within the repository. This uses the git-subdir format pointing to the `plugin/` directory.

```json
{
  "name": "ztlctl",
  "description": "Agentic research assistant for ztlctl knowledge vaults",
  "repository": "https://github.com/ThatDevStudio/ztlctl",
  "directory": "plugin",
  "license": "MIT",
  "keywords": ["zettelkasten", "knowledge-management", "research", "notes", "agentic", "mcp"]
}
```

The `"directory": "plugin"` field is the critical part — it tells Claude Code to look in the `plugin/` subdirectory for the plugin contents when installing from the git repo.

**plugin/.claude-plugin/plugin.json** — UPDATE:
- Keep all existing fields
- Verify `commands`, `agents`, `hooks` paths are correct for the new file set (commands: ./commands, agents: ["./agents"], hooks: ./hooks/hooks.json)
- No version change here — version sync handles it automatically
  </action>
  <verify>
    <automated>cd /Users/shparki/Documents/Workspace/thatdev/ztlctl && echo "=== marketplace.json ===" && cat marketplace.json && echo "=== plugin.json ===" && cat plugin/.claude-plugin/plugin.json && echo "=== marketplace.json at root (not in plugin/) ===" && test -f marketplace.json && echo "YES" || echo "NO" && echo "=== marketplace.json NOT in plugin/ ===" && test ! -f plugin/marketplace.json && echo "CORRECT" || echo "WRONG"</automated>
  </verify>
  <acceptance_criteria>
- marketplace.json exists at repo root (NOT inside plugin/)
- marketplace.json contains "directory": "plugin"
- marketplace.json contains repository URL pointing to ThatDevStudio/ztlctl
- plugin.json commands path is "./commands"
- plugin.json agents path is ["./agents"]
- plugin.json hooks path is "./hooks/hooks.json"
  </acceptance_criteria>
  <done>marketplace.json at repo root points to plugin/ subdirectory, plugin.json manifest is accurate</done>
</task>

<task type="auto">
  <name>Task 2: Add version sync step to release pipeline and rewrite plugin README</name>
  <files>.github/workflows/release-pipeline.yml, plugin/README.md</files>
  <read_first>.github/workflows/release-pipeline.yml, plugin/README.md, .planning/phases/31-commands-agents-and-distribution/31-CONTEXT.md, .planning/research/PITFALLS.md</read_first>
  <action>
**.github/workflows/release-pipeline.yml** — ADD version sync step:
Add a new step in the `release` job AFTER the "Bump version and changelog" step (id: bump) and BEFORE the "Release candidate sync dev dependencies" step (id: rc_sync_dev). The step should:

1. Only run when `steps.bump.outputs.released == 'true'` (same condition as other post-bump steps)
2. Read the version from `steps.bump.outputs.version`
3. Use `python3` (already available) to update `plugin/.claude-plugin/plugin.json` version field:
   ```yaml
   - name: Sync plugin version
     id: sync_plugin_version
     if: ${{ steps.mode.outputs.recovery != 'true' && steps.bump.outputs.released == 'true' }}
     shell: bash
     run: |
       python3 -c "
       import json
       from pathlib import Path
       p = Path('plugin/.claude-plugin/plugin.json')
       data = json.loads(p.read_text())
       data['version'] = '${{ steps.bump.outputs.version }}'
       p.write_text(json.dumps(data, indent=2) + '\n')
       "
       git add plugin/.claude-plugin/plugin.json
       git commit --amend --no-edit
   ```
   The `git commit --amend --no-edit` is critical — it amends the cz bump commit to include the plugin.json change, so the version bump and plugin sync are atomic (single commit, single tag). This is safe because the bump commit has not been pushed yet at this point.

**plugin/README.md** — REWRITE entirely:
Replace the entire file with comprehensive installation documentation. Structure:

```markdown
# ztlctl Plugin for Claude Code

Agentic research assistant for ztlctl knowledge vaults. Wraps the ztlctl MCP server with 10 deep skills, 5 slash commands, and 2 autonomous agents.

## Prerequisites

1. **Python 3.13+** with uv or pipx:
   ```bash
   # Option A: uv (recommended)
   uv tool install ztlctl
   # Option B: pipx
   pipx install ztlctl
   ```

2. **Initialize a vault:**
   ```bash
   mkdir my-vault && cd my-vault
   ztlctl init
   ```

3. **Verify the MCP server works:**
   ```bash
   echo '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{}}}' | uvx ztlctl serve
   ```
   You should see a JSON response with no errors.

## Installation

```bash
claude plugin install ztlctl
```

### Post-install verification

```bash
claude mcp list
```

Confirm `ztlctl` appears in the server list. If it does not, see Troubleshooting below.

## What you get

| Component | Count | Purpose |
|-----------|-------|---------|
| Skills | 10 | Multi-step workflow guides (orient, session, capture, etc.) |
| Commands | 5 | `/ztlctl:session`, `/ztlctl:capture`, `/ztlctl:review`, `/ztlctl:seed`, `/ztlctl:align` |
| Agents | 2 | Autonomous research and maintenance |
| Hooks | 1 | Vault gate (blocks tools when no vault found) |

## Quick start

Start Claude Code in your vault directory:

```bash
cd my-vault
claude
```

Then use slash commands:
- `/ztlctl:session "Topic"` -- start a structured research session
- `/ztlctl:capture` -- guided knowledge capture
- `/ztlctl:review` -- vault health review and triage
- `/ztlctl:seed "Quick idea"` -- zero-friction seed capture
- `/ztlctl:align "Should I use X?"` -- check decision against priorities

Or ask naturally: "research what I know about X" or "run vault maintenance" to trigger agents.

## Updating

```bash
claude plugin update ztlctl
```

Plugin versions are synchronized with ztlctl releases. Update both:
```bash
uv tool upgrade ztlctl && claude plugin update ztlctl
```

## Troubleshooting

**MCP server not showing in `claude mcp list`:**
This is a known issue. Restart Claude Code. If still missing, verify ztlctl is on PATH: `which ztlctl` or `uvx ztlctl --version`.

**Tools require approval on every call:**
Run from your vault directory (where `ztlctl.toml` exists). The vault gate hook checks for vault presence.

**Windows users:**
Ensure `uv` is on PATH (`winget install astral-sh.uv`). If `uvx` is not found by Claude Code, use the full path to uvx in your MCP config.

**Plugin loads but skills don't fire:**
Run `/reload-plugins` to refresh. For MCP config changes, restart Claude Code.
```

Per DINF-03 convention: this README IS the documentation update for this phase since it covers user-facing commands and installation.
  </action>
  <verify>
    <automated>cd /Users/shparki/Documents/Workspace/thatdev/ztlctl && echo "=== Version sync step exists ===" && grep -c "Sync plugin version" .github/workflows/release-pipeline.yml && echo "=== README has install command ===" && grep -c "claude plugin install" plugin/README.md && echo "=== README has prerequisites ===" && grep -c "Prerequisites" plugin/README.md && echo "=== README has troubleshooting ===" && grep -c "Troubleshooting" plugin/README.md && echo "=== README has mcp list verification ===" && grep -c "claude mcp list" plugin/README.md</automated>
  </verify>
  <acceptance_criteria>
- release-pipeline.yml contains a "Sync plugin version" step
- Version sync step runs after cz bump and before push
- Version sync step amends the bump commit to include plugin.json
- Version sync step has the correct conditional (recovery != true && released == true)
- plugin/README.md covers prerequisites (Python 3.13, uv/pipx)
- plugin/README.md contains `claude plugin install ztlctl` command
- plugin/README.md contains post-install verification (`claude mcp list`)
- plugin/README.md contains troubleshooting section with Windows notes
- plugin/README.md lists all 5 commands and 2 agents
- plugin/README.md contains update instructions (`claude plugin update`)
  </acceptance_criteria>
  <done>Version sync step in release pipeline atomically bumps plugin.json version with pyproject.toml, README provides complete installation and troubleshooting documentation</done>
</task>

</tasks>

<verification>
- `cat marketplace.json | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['directory']=='plugin'"` passes
- `grep "Sync plugin version" .github/workflows/release-pipeline.yml` finds the step
- `grep -A5 "Sync plugin version" .github/workflows/release-pipeline.yml | grep "plugin.json"` confirms it targets plugin.json
- `grep "claude plugin install" plugin/README.md` confirms install command documented
- `grep "Troubleshooting" plugin/README.md` confirms troubleshooting section exists
- `grep "Windows" plugin/README.md` confirms Windows notes present
</verification>

<success_criteria>
- marketplace.json at repo root enables `claude plugin install ztlctl` from GitHub
- Release pipeline automatically syncs plugin.json version with pyproject.toml on every release
- Plugin README covers full installation lifecycle: prerequisites, install, verify, update, troubleshoot
- Version sync is atomic with the cz bump commit (amend, not separate commit)
</success_criteria>

<output>
After completion, create `.planning/phases/31-commands-agents-and-distribution/31-02-SUMMARY.md`
</output>
