The vault is treated as plain Markdown with no special viewer integration.

- Prefer portable files over viewer-specific metadata.
- Keep workflow guidance inside `.ztlctl/workflow/` for predictable tooling behavior.
- Use `ztlctl export` when you need presentation-oriented views, especially `export dashboard` for review queues and topic dossiers.
