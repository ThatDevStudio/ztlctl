The vault is treated as plain Markdown.

- Prefer portable files over viewer-specific metadata.
- Keep workflow guidance inside `.ztlctl/workflow/` for predictable tooling behavior.
- Use `ztlctl export` when you need presentation-oriented views.
