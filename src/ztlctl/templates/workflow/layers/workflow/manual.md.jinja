Manual mode optimizes for direct human operation.

- Treat generated workflow notes as checklists and operating conventions.
- Run ztlctl commands directly rather than relying on agent orchestration.
- Update the scaffold only when the manual process meaningfully changes.
