Operate without source control automation.

- Treat `.ztlctl/workflow/` as local scaffolding.
- Apply updates carefully because Copier cannot rely on Git merge metadata.
- Export or back up the vault before large workflow changes.
