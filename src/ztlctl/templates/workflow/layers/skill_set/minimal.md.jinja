Minimal skill set priorities:

- keep the workflow lightweight and easy to maintain
- generate only the baseline guidance needed to operate the vault
- avoid process overhead unless the work pattern justifies it
